# backend (auth + 用户管理) 外部依赖
# 内部包 kronos-contracts 不在此声明（路径依赖）：
#   Docker:  Dockerfile 里 COPY packages/kronos-contracts 后 pip install（已存在）
#   本地:    pip install -e ../packages/kronos-contracts
fastapi>=0.100
uvicorn[standard]>=0.24
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
psycopg2-binary>=2.9
alembic>=1.13
argon2-cffi>=23.1
pyjwt>=2.8
pydantic[email]>=2.5
