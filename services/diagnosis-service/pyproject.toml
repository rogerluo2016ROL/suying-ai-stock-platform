[project]
name = "diagnosis-service"
version = "0.1.0"
description = "FastAPI diagnosis service — 5-dimensional stock analysis"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.27",
    "pyjwt",
    "sqlalchemy>=2.0",
    "asyncpg",
    "kronos-factors",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "httpx>=0.27",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["app*"]

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"
