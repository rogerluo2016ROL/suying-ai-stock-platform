# AGF Install — Next Steps

> Generated by AppGenesisForge `install-to-existing.sh` on 2026-07-02 21:45:06.
> Backup stamp: `20260702-214506`. 初始化完成后删除本文件。

## 推荐：让 Claude Code 接管初始化（一条命令）

进入项目后启动 Claude Code，运行 slash 命令：

```bash
cd /Users/rogerluo/程序目录/K线大模型
claude
```
```
/agf-init
```

`/agf-init` 由 Claude **自动完成全部初始化**：Day-1 体检（init-team.sh）→ 合并 CLAUDE.md 团队段 → 据本项目实际技术栈重写 ADR-000（brownfield 先跑 `/agf-understand`）→ 合并 settings.json 安全防御 → 建 gh label → preset 裁剪 → 清理安装残留。改你文件 / 删文件前会先展示并征求确认，技术栈与 preset 等关键决策会问你。

初始化后用 `/agf-team-start <feature>` 起第一个交付。

---

## 手工 fallback（不想用 AI 初始化时）

逐项手工做，对应 `/agf-init` 的步骤：

1. **合并 CLAUDE.md**：brownfield——旧版保留为 `CLAUDE.md`，模板版在 `CLAUDE.md.agf-template`；把模板的 `Project-Specific Rules` / `Tool Boundaries` / `Team Runtime Contract` / `Verified Facts` 段追加进你的 CLAUDE.md，保留你的 `Project Overview` / `Tech Stack`。greenfield（装前无 CLAUDE.md）——`CLAUDE.md` 已是 `CLAUDE.example.md` 脚手架，按占位提示填项目内容，团队段从 `.agf-template` 摘取。
2. **写 ADR-000**：`docs/adr/000-system-architecture.md.agf-template` 是模板默认栈（FastAPI+React+Postgres），按本项目实际 stack 重写 `docs/adr/000-system-architecture.md`。
3. **合并 settings.json**：若有 `.claude/settings.json.agf-template`，把 `permissions.deny` / `hooks` 块 / `env.CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS` 合并进你的 `.claude/settings.json`（缺则防御 / 团队功能不生效）。
4. **Day-1 体检**：`bash setup/init-team.sh`（预期全绿）。
5. **首次启动**：`bash setup/agf-team-start.sh`。
6. 完成后删除各 `*.agf-template` 与 `*.backup-20260702-214506`。

---

## 回滚（如出问题）

```bash
cd /Users/rogerluo/程序目录/K线大模型
rm -rf .claude
mv .claude.backup-20260702-214506 .claude
[ -f CLAUDE.md.backup-20260702-214506 ] && cp CLAUDE.md.backup-20260702-214506 CLAUDE.md
rm -rf setup
rm -f AGF_INSTALL_NEXT_STEPS.md
rm -rf docs/FIRST_RUN.md docs/team-capability-map.md docs/product-workflow.md
[ -f .git/hooks/pre-commit.backup-20260702-214506 ] && mv .git/hooks/pre-commit.backup-20260702-214506 .git/hooks/pre-commit || rm -f .git/hooks/pre-commit
```
