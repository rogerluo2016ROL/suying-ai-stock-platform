# AGF Install — Next Steps

> Generated by AppGenesisForge `install-to-existing.sh` on 2026-05-30 13:29:13.
> Backup stamp: `20260530-132912`. Delete this file once you finish the checklist.

## 1. 合并 CLAUDE.md（必做）

你的旧 CLAUDE.md 保留为 `CLAUDE.md`（项目专属内容仍在）。
模板版本保存在 `CLAUDE.md.agf-template`。

**合并策略**：
- 保留 CLAUDE.md 你的项目特定段：`Project Overview` / `Tech Stack` / `Quick Commands` 等
- 从 `CLAUDE.md.agf-template` **追加**这几个团队基础设施段：
  - `Project-Specific Rules`（含 SSOT 指针 / 设计交付路径 / skill 调用提醒）
  - `Tool Boundaries`（四层 hook 防御说明）
  - `Team Runtime Contract`（角色 / 工作流 / standards 索引）

完成后删除：`rm CLAUDE.md.agf-template CLAUDE.md.backup-20260530-132912`

## 2. 写 ADR-000（必做）

`docs/adr/000-system-architecture.md.agf-template` 是模板（FastAPI+React+Postgres）。
你必须基于本项目实际 stack 写自己的 `docs/adr/000-system-architecture.md`。

**推荐方式**：让 tech-lead 自动生成
```bash
cd /Users/rogerluo/程序目录/K线大模型
claude --agent tech-lead "基于现有代码（backend/ frontend/ miniapp/ 等）识别 ADR-000 基线，输出到 docs/adr/000-system-architecture.md"
```

完成后删除：`rm docs/adr/000-system-architecture.md.agf-template`

## 3. 检查 .claude/settings.json（如有 .agf-template 后缀）

如果存在 `.claude/settings.json.agf-template`，说明你的项目已有 settings.json 被保留。

模板版含完整四层防御（permissions.deny / hooks 注册 / env），你的版本可能不全。
建议参考模板把缺的部分合并进 `.claude/settings.json`：
- `permissions.deny`（必加，否则 .env / SSH 等敏感路径不受保护）
- `hooks` 块（必加，否则 PreToolUse / UserPromptSubmit 等防御不触发）
- `env.CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS`（启用 Agent Teams）

完成后删除：`rm .claude/settings.json.agf-template`

## 4. 安装 Claude Code plugins（agent skills 依赖）

进入 `/Users/rogerluo/程序目录/K线大模型` 后跑 `claude`，然后：

```
/plugin install superpowers
/plugin install code-review
/plugin install code-simplifier
/plugin install feature-dev
/plugin install frontend-design     # 如有前端
/plugin install chrome-devtools-mcp # 如做 E2E
```

## 5. Day-1 体检

```bash
cd /Users/rogerluo/程序目录/K线大模型
bash init-team.sh
```

预期全绿。失败项按提示修复。

## 6. 首次启动 Agent Team

```bash
cd /Users/rogerluo/程序目录/K线大模型
./agf-team-start.sh
```

按交互菜单选 teammate + 输入 feature 描述。

## 7.（可选）启动 team-dashboard

`tools/team-dashboard/` 是单任务只读 web 看板（FastAPI SSE + React/Vite），跑 `agf-team-start` 时可在另一个终端打开看实时进度。**零侵入**，不开也不影响团队。

首次启动：

```bash
cd /Users/rogerluo/程序目录/K线大模型/tools/team-dashboard
pip install -e ./server     # 一次性（建议进 venv）
(cd web && pnpm install)    # 一次性
./start.sh                  # 打开 http://localhost:5173/
```

---

## 回滚（如出问题）

```bash
cd /Users/rogerluo/程序目录/K线大模型
rm -rf .claude
mv .claude.backup-20260530-132912 .claude
[ -f CLAUDE.md.backup-20260530-132912 ] && cp CLAUDE.md.backup-20260530-132912 CLAUDE.md
rm -f init-team.sh agf-team-start.sh AGF_INSTALL_NEXT_STEPS.md
rm -rf docs/FIRST_RUN.md docs/team-capability-map.md docs/product-workflow.md
[ -f .git/hooks/pre-commit.backup-20260530-132912 ] && mv .git/hooks/pre-commit.backup-20260530-132912 .git/hooks/pre-commit || rm -f .git/hooks/pre-commit
```
