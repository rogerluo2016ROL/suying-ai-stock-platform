---
reviewer: code-reviewer
feature: ML-P2 (task #9, audit-model-2026-06-22 M13/M14/M15部分/M16) + M03 补提交
date: 2026-06-23
code_verdict: approve
sit_audit_verdict: "✅ Pass"
critical_count: 0
warning_count: 1
suggestion_count: 2
---

# ML-P2 代码审查报告（含 M15 follow-up 评估 + M03 补提交 + SIT Audit）

- **Task**: #9 ML-P2 模型 P2 技术债（4 个：M13/M14/M15 部分/M16）+ M03 补提交
- **上游**: `docs/reviews/audit-model-2026-06-22.md` §2 M13-M16
- **commit 链**: `d9840d4`（M13）/ `baff037`（M14）/ `1dc4c00`（M15）/ `eed099b`（M16+M03 engine）/ `a6bce3a`（M03 签名）/ `bd420d4`（M03 测试）/ `23c8137`（progress）
- **审查性质**: review-only，未改一行源码

---

## 1. 一句话 verdict

**approve** — 4 个 P2 全部正确实现：M13 向量化与原 for-loop **数值严格一致**（7 单测 `assert_allclose(rtol=0, atol=1e-6)`，非近似）/ M14 真去硬编码（sym 从 `train_data.keys()` 透传）/ M16 多 seed 纯函数（population std + 1e-12 容差防 ICIR 爆）+ M15 params.py 抽离 + re-export 兼容正确。**M15 函数级拆分标 follow-up 我认可**（`_score_bi_trend_arrays` 645 行无单测，盲拆回归风险高，先抽参数低风险高价值部分合理）。M03 补提交把 ML-P0 漏 commit 的 end_date 签名传播收尾（base/pg_adapter/_db_stub + 3 单测）。

---

## 2. 逐条核对（按 AC）

### AC-1 / M13 calc_obv/calc_wr 向量化 — **approve（数值严格一致）**

**位置**: `bi_trend_launch.py:56-99`

**核实（向量化数学正确性）**:
- `calc_obv`（line 56-73）: `direction = np.sign(np.diff(closes))` + `obv[1:] = np.cumsum(direction * volumes[1:])` —— 与递归公式 `OBV[t] = OBV[t-1] + sign(close[t]-close[t-1]) * volume[t]` **严格等价**（非近似）。边界 n=0/n=1 处理正确（return zeros(0) / zeros with obv[0]=0）。✓
- `calc_wr`（line 76-99）: `pd.Series.rolling(window=period, min_periods=period)` 右闭窗口（含当前 bar，window=i-period+1..i，与原 for-loop 一致）+ flat range (hh==ll) → 50（与原实现一致）+ 前 period-1 NaN（min_periods=period 保证）。✓

**单测质量（7 测，`test_calc_obv_wr_vectorized.py`）**:
- 对照**内联 reference for-loop** + `np.testing.assert_allclose(got, ref, rtol=0, atol=1e-6)` —— **rtol=0 atol=1e-6 是数值严格一致**（仅容浮点累加噪声），非近似。✓
- 覆盖: 随机数据 / obv[0]=0 / 简单序列 [0,200,-100,-100,400] / empty+single / 全 flat=0 / WR 前 period-1 NaN / WR flat range=50。边界全覆盖。✓
- 性能实证（progress）: calc_obv 0.3ms / calc_wr 3.8ms（50×2000 bars），从 audit M13 的 O(N×period) Python 切片提升显著。✓

---

### AC-2 / M14 score_fundamental(sym) 去硬编码 — **approve**

**位置**: `services/training-service/app/training_engine.py:245-295`（caller `_build_features_from_kline`）+ `:976-982`（sym 来源）

**核实**:
- `_build_features_from_kline(sym: str = "000001")` 签名加 sym 参数（line 245），默认 000001 **仅 legacy/无上下文 caller** 用，生产 caller（train 路径）始终传真实 sym（注释 line 252-255 明示）。✓
- caller（line 976-982）: `for sym in symbols: ... sym=sym` where `symbols = list(train_data.keys())[:max_stocks]` —— **真实每股 code 透传**，非硬编码常量。✓
- line 295 `fund = score_fundamental(sym)`（生产 `score_fundamental` 从 `webui.services.screener_service` import，line 258）—— 不同 sym → 不同基本面数据 → 不同 fund_score，模型有了真实特征信息量。✓

**单测（3 测，`test_m14_score_fundamental_sym.py`）**:
- `test_build_features_signature_accepts_sym`: 签名契约。✓
- `test_build_features_passes_sym_to_score_fundamental`(monkeypatch): 传 000001 + 600519 两次，断言 `score_fundamental` 收到两个不同 code（seen_codes 含两者）。**行为级验证透传，非硬编码。** ✓
- `test_fund_score_varies_across_symbols`(monkeypatch): mock score_fundamental 返回 000001→7.0 / 600519→3.0，断言两次 fund_score 列不同。**验证去恒常数。** ✓

---

### AC-3 / M15 bi_trend_launch.py 拆分 — **部分达成 + follow-up 我认可**

**位置**: `bi_trend_launch.py`（2168→1961 行，-214）+ `engine/params.py`（新 230 行）+ `test_m15_params_extraction.py`（4 测）

**核实（params.py 抽离）**:
- `params.py` 集中全部可调参数常量（WEIGHTS / OBV/WR/ADX 阈值等）+ M02/M09 DEPRECATED 标注完整保留（line 86-114, 170-171 风格延续）+ Vxx 版本演化注释保留。✓
- **re-export 兼容**: `bi_trend_launch.py:30-32` `from kronos_factors.engine.params import *` + 显式 alias（`from kronos_factors.engine import params as _params`）—— 外部 `from bi_trend_launch import WEIGHTS` **不破**。✓
- 下游消费者 import 验证（progress）: bi_trend_full_market / walk_forward / backtest_bi_trend 全部 import OK，无回归。✓

**单测（4 测，`test_m15_params_extraction.py`）**: params 模块导入 + 参数常量存在 + re-export 兼容 + DEPRECATED 标注保留。✓

#### M15 follow-up 评估（我独立判断: **认可 defer**）

ml-engineer 把"函数级拆分（factors/scoring/screening）"标 follow-up，理由是 `score_bi_trend`/`_score_bi_trend_arrays`/`run_bi_screening` 深度互调 + 引用上百常量。我独立核实:
- **`_score_bi_trend_arrays` 是 645 行单函数**（bi_trend_launch.py:838-1483，`awk` 核实）—— audit M15 要求拆成 factors.py/scoring.py/screening.py 的核心对象。
- **该函数当前零单测**（私有函数，需 DB/prefetch 上下文才能调起，测试 fixture 成本高）。
- **盲拆 645 行无测试覆盖的函数 = 高回归风险**（audit §M15 自己也指出"任何一处改参数都需 grep 全文确认无副作用"是 M02/M09 治理难的结构性原因——拆分本身要解决这个，但拆分动作若无测试保护会先引入新 bug）。

**判定**: params.py 抽离（低风险高价值，集中可调参数便于 M02/M09 治理）已完成且正确；函数级拆分标 follow-up **合理**，建议挂 bi_trend 重设专项（progress 已建议与 issue #2 gitlink / phase1 样本外重设同批），**前置条件 = 先补 `_score_bi_trend_arrays` 集成测试覆盖**。我认可 ml-engineer 的"先测试再拆"顺序，不要求本 task 强行拆分。

---

### AC-4 / M16 run_historical_backtest 多 seed — **approve（纯函数实现扎实）**

**位置**: `backtest/engine.py:402-671`（run_historical_backtest 重构）+ `:469-628`（_run_one_seed 闭包）+ `:687-710`（_aggregate_multi_seed 纯函数）

**核实**:
- `run_historical_backtest(..., n_seeds: int = 5)`（line 405），默认 5，n_seeds=1 退回 legacy 单 seed 路径（line 421 注释）。✓
- `_run_one_seed(seed)` 闭包（line 469）: 固定 seed 驱动 `random.seed(seed)`（line 475），跑一次完整 sampling pass 返回 per-seed 累加器。✓
- `_aggregate_multi_seed(seed_mean_ics) → {name: {seed_mean_ic, seed_std_ic, seed_icir, n_seeds}}`（line 687-710）—— **纯函数**（无 I/O 无全局状态）:
  - `s = float(np.std(arr))` —— **ddof=0 population std**（默认），符合文档"population std"。✓
  - `seed_icir = m / s if s > 1e-12 else 0.0` —— **1e-12 容差防 fp 噪声**（identical ICs 的 std ~1e-17 而非 0，无容差会使 ICIR 爆到 1e16+）。✓ 这是 audit M16 + team-lead 重点，实现正确。
  - identical seeds → ICIR=0.0（非 +inf），合理。✓
- 每 model 输出 `seed_mean_ic/seed_std_ic/seed_icir/n_seeds`（line 669-672）—— cross-seed 稳定性可观测，单 lucky seed 无法伪装 robust IC。✓

**单测（4 测，`test_m16_multi_seed.py`）**: 签名 n_seeds 默认 ≥5 + aggregate 基本（mean/std + identical→0）+ ICIR divide-by-zero guard（std=0→0）+ n_seeds field。**行为级纯函数测试。** ✓

---

### M03 补提交（a6bce3a + bd420d4 + eed099b engine 调用点）— **approve（ML-P0 漏 commit 收尾）**

**核实**:
- `a6bce3a`: `base.py`（DBAdapter.get_kline + MarketDataAdapter.get_kline_df 签名加 end_date，+19/-5）+ `scorer/_db_stub.py`（StubMarketDataService.get_kline_df 同步，+2/-1）。✓ 这是 ML-P0 review §2 M03 已核实"抽象基类接口同步"的**漏 commit 收尾**（ML-P0 时 base.py/_db_stub.py 的签名改动未进 commit，本 task 补上）。
- `bd420d4`: `test_pg_adapter_end_date.py`（3 单测，73 行）—— ML-P0 review §2 M03 已核实存在的单测文件，本 task 补 commit。✓
- `eed099b` 含 `backtest/engine.py:498-506` run_historical_backtest 调用 `get_kline_df(code, lookback=400, end_date=batch_date)` —— ML-P0 review §2 M03 已核实。✓

**判定**: M03 补提交是 ML-P0 review 时已核实内容正确、但 commit 链漏掉的文件收尾。无新代码逻辑，3 单测全绿（progress: test_pg_adapter_end_date.py 3 passed）。✓

---

## 3. commit 纪律核实

7 个 ML commit 全 per-file 零夹带:
- `d9840d4`: 仅 bi_trend_launch.py + test_calc_obv_wr（2 files）
- `baff037`: 仅 training_engine.py + test_m14（2 files）
- `1dc4c00`: 仅 bi_trend_launch.py + params.py + test_m15（3 files）
- `eed099b`: 仅 backtest/engine.py + test_m16（2 files，含 M03 engine 调用点）
- `a6bce3a`: 仅 base.py + _db_stub.py（2 files）
- `bd420d4`: 仅 test_pg_adapter_end_date.py（1 file）

BE 的 `37040d9`/`3d3cc3e`/`c00bf32`（BE-P0/P1/P2）在 commit 链间但**独立 commit，非 ml-engineer 混入**——核实未触碰（每个 ML commit 的 --stat 都不含 BE 文件）。✓

---

## 4. Findings 汇总

### Critical
无。

### Warning

| # | 位置 | 问题 | 修复 |
|---|---|---|---|
| W-1 | `test_short_mode_engine_weights` fail（预存） | kronos-factors `pytest tests/` = 37 passed **1 failed**，fail 为 `test_short_mode_engine_weights`（modes.py:203 `short_term: 0.28` ≠ 测试期望 0.30）。**非 ML-P2 引入**（grep 确认 7 个 ML commit 均未触 modes.py）。 | follow-up: 该 fail 属 BE/scoring 范围（modes.py 权重表），建议派回 backend-dev 或单独立项修测试/代码对齐。不阻断 ML-P2。 |

### Suggestion

| # | 位置 | 建议 |
|---|---|---|
| S-1 | `_score_bi_trend_arrays`（bi_trend_launch.py:838-1483，645 行） | M15 follow-up 前置: 补该私有函数的集成测试覆盖（当前零单测）。建议 fixtures 注入 mock kline 数组直接调（无需 DB），断言评分输出形状/范围。补完后再做 factors/scoring/screening 拆分。 |
| S-2 | `_aggregate_multi_seed`（engine.py:700） | `np.std(arr)` 默认 ddof=0（population），但 M16 文档措辞"cross-seed std-of-means"在统计惯例上常用 ddof=1（sample std，n_seeds 小时差异明显）。当前实现注释写"population std"与代码一致，可接受；若后续 n_seeds 增大需做置信区间，建议显式选 ddof。非阻断。 |

---

## 5. 认可的设计

- **M13 向量化数值严格性**: 7 单测用 `rtol=0, atol=1e-6` 对照内联 reference for-loop，是"严格一致非近似"——audit M13 要的是"重写不改变数值"，这个测试标准正好守住。性能实证（0.3ms/3.8ms）也落地。
- **M14 行为级去硬编码验证**: 不只验签名，而是 monkeypatch 验 sym 真透传 + 不同 sym 产生不同 fund_score——直接证伪"恒常数"缺陷。
- **M16 fp 安全的纯函数**: `_aggregate_multi_seed` 抽纯函数（可单测）+ 1e-12 容差防 identical-IC ICIR 爆——这是 audit M16 + team-lead 点名的细节，实现到位。n_seeds=1 退回 legacy 路径保证向后兼容。
- **M15 follow-up 决策诚实**: params.py（低风险高价值）做完，函数级拆分（高风险）诚实标 follow-up 并给前置条件（先补测试）+ 归口（bi_trend 重设专项），不过度承诺。`_score_bi_trend_arrays` 645 行的实测支撑了 defer 决策。
- **M03 补提交纪律**: ML-P0 漏 commit 的 base.py/_db_stub.py 签名 + 单测，本 task 独立 commit 收尾，不混入 M16。commit message 明示"ML-P0 已批漏 commit 的契约收尾"。
- **commit per-file 零夹带**: 7 commit 每个原子化，BE commit 在链间但不混入。

---

## 6. SIT Audit

### 4 项 audit 检查

1. **progress 完整性**: `progress/ml-engineer.md` 含完整 `## ML-P2 (task #9)` 段，按 AC 列条目（AC-1~5）+ 行首 `[x]/[~]` 自验勾选 + 每条附 commit hash + 测试计数 + 质量门 + 产物 commit 链。M15 部分达成用 `[~]` 诚实标注 + follow-up 说明。✓
2. **AC 覆盖**: 全部 5 AC（M13/M14/M15 部分/M16 + AC-5 pytest）逐条覆盖；M15 标 `[~]` 部分达成 + follow-up 解释（不是漏覆盖，是诚实标注）；M03 补提交作为 AC-4 附带项说明。✓
3. **证据可信度**:
   - pytest 真实输出: kronos-factors `37 passed, 1 failed` + training-service `32 passed, 1 skipped` + 18 新测全绿（M13 7+M14 3+M15 4+M16 4）+ benchmark 数字（calc_obv 0.3ms/calc_wr 3.8ms）。**真实工具真实输出片段。** ✓
   - **行为级单测为主**: M13（reference 对照 allclose）/ M14（monkeypatch 验透传+变值）/ M16（纯函数+fp 容差）全是行为级，非契约字符串嗅探。ML-P1 提的"M01/M02/M05 缺行为级"concern 在 M13/M14/M16 这里正面回应。✓
   - M15 re-export 兼容有下游 import 验证（bi_trend_full_market/walk_forward/backtest_bi_trend）。✓
4. **失败/阻塞标记真实性**: 1 failed（test_short_mode_engine_weights）**如实标记 + 定性为"预存 P2 失败与本 commit 无关" + grep 确认 modes.py:203 非 ML-P2 触及**——我独立 grep 7 commit 均未触 modes.py，坐实预存。1 skipped（PG）如实标。M15 部分达成用 `[~]` 非伪装 `[x]`。无 fail 伪装 pass。✓

### SIT Audit verdict: **✅ Pass**

4 项全过:
- progress 完整 + AC 全覆盖（含 M15 诚实部分达成标注）
- 证据真实（pytest 输出 + benchmark + 行为级单测）
- 失败如实标记 + 预存定性有 grep 实证（我独立复核 7 commit 未触 modes.py）
- 唯一 fail 是预存 BE 范围（W-1），不属 ML-P2 引入，不阻断

无需 product-lead 决定补救。

---

## 7. 架构评估

- **M13/M14/M16 无架构风险**: 纯性能/正确性/可观测性改进，实现扎实，无新架构债。
- **M15 部分达成的架构意义**: params.py 抽离是 audit M15 "使 M02/M09 过拟合治理需 grep 全文确认副作用"的**部分缓解**——可调参数集中后，调参审计只需查 params.py 一处。函数级拆分（彻底解决）标 follow-up，归口 bi_trend 重设专项合理（bi_trend 已定性需根本重设，在其上做大规模结构重构属浪费）。
- **M03 补提交无架构影响**: 纯契约收尾。

---

<!-- agf-verdict 机读块（validate-review-verdict.sh 守门依赖；计数须与 frontmatter 一致）
critical: 0
warning: 1
suggestion: 2
code_verdict: approve
sit_audit_verdict: ✅ Pass
verdict_derivation: |
  code_verdict=approve 因为: 4 个 P2 全部正确实现 — M13 向量化 (np.sign+np.diff+cumsum / pd.rolling 右闭窗) 与原 for-loop 数值严格一致 (7 单测 assert_allclose rtol=0 atol=1e-6 非近似 + 边界全覆盖 + 性能实证) / M14 真去硬编码 (sym 从 train_data.keys() 透传, monkeypatch 验不同 sym 不同 fund_score) / M16 多 seed 纯函数 (population std + 1e-12 容差防 ICIR 爆 + n_seeds=1 退回 legacy + 4 行为级单测) / M15 params.py 抽离 + re-export 兼容正确 (4 单测 + 下游 import 验证)。M15 函数级拆分 (_score_bi_trend_arrays 645 行无单测) 标 follow-up 我认可 (先测试再拆顺序合理, 归口 bi_trend 重设专项)。M03 补提交收尾 ML-P0 漏 commit (base/_db_stub 签名 + 3 单测)。commit 7 per-file 零夹带。
  sit_audit_verdict=✅ Pass 因为: 4 项全过 — progress 完整 (AC 行首勾选+commit hash+测试计数) / AC 全覆盖 (M15 [~] 诚实部分达成非漏覆盖) / 证据真实 (pytest 37p+32p+18 新测 + benchmark + 行为级单测为主回应 ML-P1 concern) / 失败如实标记 (test_short_mode_engine_weights 预存, grep 确认 7 commit 未触 modes.py)。
  架构: M13/M14/M16 无风险; M15 params 抽离部分缓解过拟合治理痛点, 函数级拆分 follow-up 归口合理; M03 无架构影响。
-->
