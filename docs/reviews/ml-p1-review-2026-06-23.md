---
reviewer: code-reviewer
feature: ML-P1 (task #8 M07-M12) + M01-A/C 强化 (ML-P0 收尾, tech-lead 派单)
date: 2026-06-23
code_verdict: approve
sit_audit_verdict: "⚠️ Pass with concerns"
critical_count: 0
warning_count: 2
suggestion_count: 2
---

# ML-P1 代码审查报告（含 M01-A/C 收尾 + gitlink 止血 + SIT Audit）

- **Task**: #8 ML-P1 模型 P1 工程债（6 个 M07-M12）+ ML-P0 收尾（M01-A/C 强化 + W-1 测试隔离 + M11 gitlink）
- **上游**: `docs/reviews/audit-model-2026-06-22.md` §2 M07-M12 + `docs/reviews/m01-techlead-assessment-2026-06-22.md` §3
- **commit 链**: `e7f13ad`（W-1）/ `7031fdb`（M07-M12）/ `92a4d39`（M01-A+C）/ `597ede8`（M11 gitlink）
- **审查性质**: review-only，未改一行源码

---

## 1. 一句话 verdict

**approve** — 6 个 P1 AC（M07-M12）全部正确实现 + **M01-A/C 强化直接消解 ML-P0 的 C-1 critical**（`_timeline_guard_decision` 纯函数 + dirty 始终 exit(2) + strict 模式 commit 日期>OOS 起始 exit(2) + 8 行为级单测验 sys.exit 真触发）+ W-1 测试隔离彻底解决（hack 清零，51 passed/0 ImportError）+ M11 gitlink 零夹带止血。**C-1 可降为 warning/resolved**。唯一 concern：M11 嵌套 commit 的 remote "my" 可达性在审查环境无法独立验证（repo 仅配 origin）。

---

## 2. M01-A/C 专项（C-1 降级判断）— **C-1 resolved，可降 warning**

**这是对 ML-P0 review (`docs/reviews/ml-p0-review-2026-06-22.md`) C-1 critical 的直接收尾。**

### ML-P0 C-1 原始问题（已 supersede）
> `_git_strategy_commit` 只记录 + 打印警告，无机制阻断用 HEAD 参数测过去。

### M01-A/C 实现（commit `92a4d39`）

**位置**: `tools/walk_forward.py:86-124`（`_timeline_guard_decision` 纯函数）+ `:236-238`（`--strict-timeline` flag）+ `:264-274`（main 调用 + sys.exit + D 模式软警告兜底）

**核实**:
1. `_timeline_guard_decision(strategy_info, start_month, strict) → {"exit": False} | {"exit": True, "code": 2, "message": ...}` —— 纯函数，可单测。✓
2. **M01-C（始终强制，不受 flag 控制）**: `if strategy_info.get("dirty"): return exit(2)`（line 104-111）。dirty 工作区跑样本外任何情况不可信，无兼容必要。✓ 这正是 ML-P0 C-1 修复建议"至少 dirty=True 时 raise"。
3. **M01-A（受 `--strict-timeline` 控制）**: `if strict and commit_date > start_month: return exit(2)`（line 113-123）。这正是 ML-P0 C-1 修复建议方案 A（strict exit）。
4. main() 真执行: `guard = _timeline_guard_decision(...)` → `if guard["exit"]: print(guard["message"]); sys.exit(guard["code"])`（line 266-269）。**真 sys.exit，非警告。** ✓
5. **D 模式过渡兜底**: 非 strict + 晚 commit → 软警告（line 270-274），保留向后兼容。✓ 合理。

**8 行为级单测**（`services/training-service/tests/test_walk_forward_timeline.py`，直接回应 ML-P0 W-2）:
- 5 纯函数场景: (a) clean+早commit+strict 放行; (b) clean+晚commit+strict exit2 + 错误信息含日期/泄露/flag 提示; (c) dirty 无论 strict exit2; (d) **dirty 优先于晚commit**（dirty+晚commit+strict 报 M01-C 非 M01-A）; (e) clean+晚commit 非 strict 放行。
- main() 级 end-to-end: mock subprocess.run 验证 `sys.exit(2)` 真触发（strict 路径 + dirty 路径）。**行为级，非契约字符串嗅探。** ✓

**判定**: **C-1 resolved**。ML-P0 C-1 的两个修复建议（strict exit + dirty raise）都已实施，且技术深度超过原始建议（抽纯函数可测 + 优先级语义 + D 模式兜底）。**可降为 warning/resolved**。

**tech-lead 评估合理性**: `m01-techlead-assessment-2026-06-22.md` §3 选 A+C 而非 B（per-oos-month checkout）的技术推理成立——bi_trend 已被证伪有 alpha（V5.9 调参前仍 -2.993），花高工程代价把"伪样本外"修成"真样本外"结论不变；A+C 逼操作者显式声明 commit + 防 dirty，是务实方案。该护栏是**跨策略的**（注释明示"价值延续到 bi_trend 重设之后"），设计正确。

---

## 3. M07-M12 逐条（ML-P1 六个 P1 AC）

### AC-1 / M07 prediction load strict — **approve**

**位置**: `services/prediction-service/app/main.py:77-120`

- `tokenizer.load_state_dict(..., strict=False)` + `model.load_state_dict(..., strict=False)`（line 80, 99），返回 `missing_t/unexpected_t` + `missing_p/unexpected_p` 记录。✓
- `_model_checkpoint_status` metric（"finetuned"/"base_public"/"not_loaded"，line 117/127）。✓
- 注: AC-1 写 "strict=False"（而非 strict=True）是**正确**的——audit M07 要求的是"记录 missing/unexpected keys 区分完全 vs 部分加载"，strict=True 会在 shape 不匹配时直接抛异常无法记录。strict=False + 记录 + status metric 是 audit 的本意。✓

### AC-2 / M08 单日口径前视标注 — **approve**

**位置**: `tools/backtest_bi_trend.py:482-484`（stdout）+ `:667-669`（JSON export）

- stdout: `⚠️ 单日口径含成交假设前视 (T收盘买入不可成交), 仅对比用, 禁止对外披露`（line 484）。✓
- JSON export `mode`/warning 段: "单日口径 (T收盘买 T+1收盘卖) 含成交假设前视 — T日 close 物理上不可成交"（line 667-669）。✓
- `ret_key = "gross_return" if multi_day else "next_day_return"`（line 462）双口径区分保留。✓

### AC-3 / M09 个股教训 DEPRECATED — **approve**

**位置**: `packages/kronos-factors/kronos_factors/engine/bi_trend_launch.py:86-87, 101-114, 170-171`

- 全部"个股事件反推"阈值标 `# DEPRECATED: in-sample anecdote (M09)` + "待 walk-forward 校准"注释。✓
- line 101 注释刻意"具体标的见审计报告，不在此列出以保持代码中性"——好的去个股化实践（避免代码里再次硬编码股票名）。✓
- 数值保留（OBV_NEGATIVE_SKIP=True / HIGH_VOL_OBV_MULT=0.6 等）但标 DEPRECATED——符合 audit "标 DEPRECATED 回学术默认"（数值层面保留 + 标注待校准，非激进清零引入回归）。✓

### AC-4 / M10 ONNX 死代码删除 — **approve**

**位置**: `services/prediction-service/app/onnx_optimizer.py`（**DELETED**）

- 文件已删（`ls` 确认 No such file）。✓
- repo-wide grep `onnx_optimizer` 仅命中 `services/training-service/tests/test_ml_p1_sit.py`——该命中是**删除断言**（`test_m10_onnx_optimizer_deleted` / `test_m10_onnx_no_callers` grep 验证），非 import 调用。✓
- CLAUDE.md / 设计文档 ONNX 措辞清理（test_m10_onnx_wording_removed_from_tech_stack 验证）。✓

### AC-5 / M11 dataset.py 时间一致性校验 — **approve（实现扎实）**

**位置**: `Kronos/Kronos-uat-bak/src/kronos/finetune/dataset.py:45-49, 84-137`（嵌套 commit `c2bc93d`）

- `_assert_no_time_overlap_with_train`: val 加载时从 train_data.pkl 取所有 symbol 最大 datetime，从当前 val 取最小 datetime，`if not (train_max < val_min): raise RuntimeError(...)`（line 126-134）。✓
- 错误信息含诊断: train max / val min / prepare_finetune_data.py 按时间切指引。✓
- 数据格式不符时 graceful skip（line 137-140）+ print——合理兜底（不同数据格式不硬崩）。✓
- **这是 audit M11 的核心要求**（防止 sliding window 重叠使 val loss 低估），实现正确。✓

### AC-6 / M12 auto-deploy 显著性门 — **approve（诚实 todo 型，非完美但合规）**

**位置**: `services/training-service/app/training_engine.py:1068-1097`

- `_evaluate_vs_production` 仍是**点估计阈值**（MIN_SIGNAL_PCT=5，line 1081，原 2% 太松），**非真 Diebold-Mariano 统计显著性检验**。⚠️
- **但诚实标注**: `significance_method: "point_estimate_threshold (NOT statistically significant)"` + `significance_todo: "upgrade to bootstrap IC CI / Diebold-Mariano once per-batch IC series persisted"`（line 1096-1097）。✓
- **风险已由 M04 兜底**: `_execute_training` 的 `MLFLOW_MODE != "live"` 安全门（ML-P0，line 864-871）无条件跳过 mock MLflow 下的 auto-deploy——即便 M12 点估计阈值误判 new_better，非 live 模式不会真上线。✓
- 判定: P1 AC 要求"阈值改统计显著性"，当前实现是"阈值收紧 + 诚实标注 + M04 兜底"，**非完美但风险已闭环**。真正 DM 检验需 per-batch IC 序列持久化，列 follow-up 合理。这是诚实的工程权衡，不阻断。

---

## 4. gitlink 止血评估（commit `597ede8`）

**核实**:
1. **零夹带**: `git show 597ede8 --stat` = `Kronos | 2 +-`（1 file changed, 1 insertion, 1 deletion）。**仅 gitlink ref，无 auth_service/engine/audit 等顶层改动混入**（工作区现存的 modified auth_service/ADR-005 等是后续未提交工作，与本 commit 无关）。✓
2. **gitlink ref 正确提升**: `git ls-tree HEAD Kronos` = `160000 commit c2bc93d...`（原 1472f20）。✓
3. **嵌套 commit 真实**: `git -C Kronos/Kronos-uat-bak log c2bc93d -1` 成功，message 含 "M11 dataset.py 加载校验 train max datetime < val min datetime"。✓
4. **备份 tag 存在**: `pre-m11-gitlink-bump` → e15f24d（回滚点就位）。✓
5. **remote "my" 可达性 — 无法独立验证**: 审查环境 `git remote -v` 仅配 `origin`（github.com/...suying-ai-stock-platform），**无 `my` remote**。team-lead 声称 "`git ls-remote my master` = c2bc93d 可达" 在本 clone 无法复现（`fatal: 'my' does not appear to be a git repository`）。

**判定**: gitlink 止血 commit 本身**正确且零夹带**，结构债（孤儿 gitlink + 路径错位）已记 issue #2（FU-Kronos-gitlink-1，触发根治 = bi_trend 重设专项）。**唯一未闭环项**：remote "my" 可达性在审查环境无法验证——可能是 remote 配置在 dev 本地未同步到 review clone，或 push 尚未完成。**建议 product-lead/ml-engineer 补证**: 在配了 `my` remote 的环境跑 `git ls-remote my master` 输出 c2bc93d 截图/日志。不阻断 merge（gitlink ref 本身正确），但作为 SIT concern 记录。

---

## 5. W-1 测试隔离 — **彻底解决（ML-P0 W-1 resolved）**

**核实**:
- `backend/tests/ml/test_group_split.py` **已删除**（`ls` = No such file），迁至 `services/training-service/tests/test_group_split.py`。✓
- 迁移后测试用**干净 import**: `from app.training_engine import _group_split_masks`（line 13），由 `services/training-service/tests/conftest.py` 把 training-service 目录加 sys.path。✓
- **hack 清零**: grep `sys.path.insert|sys.modules.pop|_SVC` 仅命中 docstring line 10（说明"不再用 hack"），代码无残留。✓
- `test_ml_p0_sit.py` 仍用文件读 + AST（避免 import training-service app），与 W-1 修复一致。✓
- 验证计数: backend `51 passed, 9 skipped, 0 failed, 0 ImportError`；training-service `29 passed, 1 skipped (PG)`。✓

**判定**: W-1 彻底解决。ML-P0 review W-1 resolved。

---

## 6. Findings 汇总

### Critical
无（ML-P0 C-1 由 M01-A/C 收尾 resolved）。

### Warning

| # | 位置 | 问题 | 修复 |
|---|---|---|---|
| W-1 | M11 gitlink remote "my" 可达性 | `git remote -v` 审查 clone 仅配 origin，无 `my` remote，team-lead 声称的 `git ls-remote my master = c2bc93d` 无法复现。gitlink ref 本身正确，但"嵌套 commit 已 push 到可达 remote"未闭环验证。 | ml-engineer 在配了 `my` 的环境补 `git ls-remote my master` 输出截图/日志到 progress；或确认 `my` 是 dev 本地 remote 未同步 review clone（属配置差异非缺陷）。 |
| W-2 | M12 `_evaluate_vs_production` 仍点估计 | 非 Diebold-Mariano 真显著性检验，当前靠收紧阈值 + 诚实标注 + M04 live-MLflow 兜底。 | follow-up: per-batch IC 序列持久化后升级 bootstrap IC CI / Diebold-Mariano（progress 已列）。M04 兜底下非阻断。 |

### Suggestion

| # | 位置 | 建议 |
|---|---|---|
| S-1 | `tools/walk_forward.py:115` | `_timeline_guard_decision` 内 `commit_date > start_month` 是字符串字典序比较（"2026-06-10" vs "2024-01"），YYYY-MM-DD / YYYY-MM 格式碰巧正确但建议显式 parse 为 date 比较，防格式漂移（如 commit_date 含时间部分 "2026-06-10T..." 截断后比较）。非阻断。 |
| S-2 | ML-P0 `test_ml_p0_sit.py` 仍 8/9 契约字符串校验 | ML-P1 已为 M01 补行为级测试（test_walk_forward_timeline.py），但 M02/M05 仍仅契约校验。建议 follow-up 补: M02 真调 run_bi_screening 断言返回 pick hold_days==5；M05 真起 prediction-service lifespan 断言 checkpoint_status。非阻断。 |

---

## 7. 认可的设计

- **M01-A/C 纯函数 + 优先级语义**: `_timeline_guard_decision` 抽成纯函数（不依赖进程状态），dirty 优先于时序泄露，D 模式过渡兜底——工程深度超过原始 C-1 建议，且 8 行为级单测覆盖全路径。直接消解 ML-P0 C-1。
- **tech-lead A+C 决策的技术诚实**: assessment §3 明确论证"bi_trend 已证伪有 alpha，B（per-oos-month checkout）修的是注定推倒重来的框架里的梁"——不 over-engineer，护栏价值延续到策略重设之后。
- **W-1 根治**: 不只是改测试 import，而是**物理迁移**到 services/training-service/tests/（conftest.py 提供 sys.path），彻底消除 backend/training-service app 命名空间冲突。hack 清零有 grep 证据。
- **M11 dataset.py 实现扎实**: 时间一致性校验是 audit M11 核心要求，train max < val min 硬断言 + 诊断信息 + 格式不符 graceful skip。
- **M12 诚实 todo**: 不伪装点估计是显著性检验，显式标 `NOT statistically significant` + todo，配合 M04 兜底闭环风险。
- **commit 纪律**: 597ede8 零夹带（仅 gitlink ref），备份 tag 就位，结构债记 issue #2 触发根治。

---

## 8. SIT Audit

### 4 项 audit 检查

1. **progress 完整性**: `progress/ml-engineer.md` 含 3 个完整 SIT 证据段——ML-P1 M07-M12（line 256+）、M01-A/C（line 315+）、ML-P0 收尾 W-1。每段含范围/运行命令/结果/质量门。✓
2. **AC 覆盖**: 全部 7 AC（M07-M12 + AC-7）逐条标 ✅ 附测试名；M01-A/C 单独 5 子 AC（AC-M01-A/C + 5 单测场景）。M11 gitlink 止血 4 步（代码+SIT+嵌套 commit+gitlink 提升）全标 ✅。✓
3. **证据可信度**:
   - pytest 真实输出片段: backend `51 passed, 9 skipped, 0 failed, 0 ImportError` + training-service `29 passed, 1 skipped (PG)` + 修复前后对比（"修复前 71 passed + 1 failed"）。**真实工具真实输出**。✓
   - **M01-A/C 行为级单测**（直接回应 ML-P0 W-2 concern）: 5 纯函数 + main() end-to-end mock subprocess 验 sys.exit 真触发——**行为级，非字符串嗅探**。✓ ML-P0 W-2 concern 在此解决。
   - M11 dataset.py 时间校验有代码实证（c2bc93d 的 dataset.py:126-134 raise RuntimeError）。✓
4. **失败/阻塞标记真实性**: M11 commit 路径 blocked → 升级 tech-lead 选项 A vs B → 选 A 实施，全过程如实记录（line 239 "commit 路径 blocked"）；9 skipped（PG）+ M11 PG skip 均如实标。无 fail 伪装 pass。✓

### SIT Audit verdict: **⚠️ Pass with concerns**

4 项主体通过，concern:
- **W-1（remote "my" 可达性未闭环）**: gitlink ref 正确但嵌套 commit push 到可达 remote 在审查环境无法验证。建议 ml-engineer 补 `git ls-remote my master` 截图，或在 progress 注明 `my` 是 dev 本地 remote。
- **M12 点估计非显著性**（W-2）: 风险由 M04 兜底闭环，非阻断，但 follow-up 需补真 DM 检验。

是否需 product-lead 决定补救: **否**——代码修复正确性已坐实，concern 不阻断 merge（W-1 是验证缺口非代码缺陷；W-2 已 follow-up）。若 PL 要求 gitlink remote 可达性硬闭环，可派回 ml-engineer 补 ls-remote 证据。

---

## 9. 架构评估 + 升级建议

**M01（策略根因 A）无需再升级**: M01-A/C 强化已采纳 tech-lead 评估的方案 A+C，机制阻断到位（dirty 始终 exit + strict 时序泄露 exit），行为级单测覆盖。tech-lead 选 A 而非 B 的技术推理（bi_trend 无 alpha，B over-engineering）成立。**ML-P0 review 的 M01 升级建议已闭环**。

**M07-M12 无架构级风险**: 六个 P1 均为工程债修复，实现正确，无新架构债引入。

**gitlink 结构债（issue #2）**: 孤儿 gitlink + 路径错位（Kronos/ 仓库根在 Kronos-uat-bak 深一层）是预存结构债，本次仅止血（update-index 提升 ref），根治（FU-Kronos-gitlink-1/2）正确地绑到 ML-P2 / bi_trend 重设专项，不在本 task。合理。

---

<!-- agf-verdict 机读块（validate-review-verdict.sh 守门依赖；计数须与 frontmatter 一致）
critical: 0
warning: 2
suggestion: 2
code_verdict: approve
sit_audit_verdict: ⚠️ Pass with concerns
verdict_derivation: |
  code_verdict=approve 因为: ML-P1 六个 P1 AC (M07-M12) 全部正确实现 (M07 strict load + missing/unexpected keys + metric / M08 单日口径前视双标注 stdout+JSON / M09 个股教训 DEPRECATED 全标 / M10 onnx_optimizer.py 删净 0 调用者 / M11 dataset.py train max<val min 硬断言 / M12 点估计收紧 + 诚实 todo + M04 兜底闭环) + M01-A/C 强化直接消解 ML-P0 C-1 critical (纯函数 _timeline_guard_decision + dirty 始终 exit(2) + strict 时序泄露 exit(2) + 8 行为级单测验 sys.exit 真触发) + W-1 测试隔离彻底解决 (hack 清零, 51 passed/0 ImportError) + M11 gitlink 零夹带止血 (仅 gitlink ref, 备份 tag 就位)。无 critical。
  sit_audit_verdict=⚠️ Pass with concerns 因为: 4 项 audit 主体通过 (progress 完整 / AC 全覆盖 / 证据真实 pytest 输出 51+29 passed / 失败阻塞如实标记 M11 blocked→升级 tech-lead), concern = W-1 remote "my" 可达性审查环境无法验证 (gitlink ref 本身正确) + W-2 M12 点估计非显著性 (M04 兜底闭环, follow-up 补 DM)。
  架构升级: M01 无需再升级 (A+C 已实施, tech-lead 评估成立); M07-M12 无架构风险; gitlink 结构债正确绑 ML-P2/bi_trend 重设。
-->
