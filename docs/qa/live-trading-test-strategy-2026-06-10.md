# QA Report -- 实盘交易 -- E2E + UAT 测试策略

- **Date**: 2026-06-10
- **Stage**: Strategy（E2E + UAT 策略框架，非执行报告）
- **Tester**: qa-engineer (deepseek-v4-pro)
- **Branch**: feature/live-trading（预计）
- **Environment**: local docker-compose（PostgreSQL + FastAPI backend + React frontend）+ 模拟券商网关（Mock Broker Gateway）
- **PRD**: Kronos/docs/投资管理平台_PRD_产品需求文档.md SS3.11（AC-11.1~11.9）
- **Mock 券商**: xtquant/QMT Mock Adapter（不连接真实券商生产环境）
- **Code review（含 SIT Audit）**: TBD（code-review 通过后方可执行本策略中的测试）

---

## Summary

> 本文件为**实盘交易 E2E + UAT 测试策略框架**，未执行。E2E 测试在 code-review + SIT Audit 通过后启动；UAT 在 E2E 通过后启动。实盘交易与模拟交易复用同一套界面（`Trade.tsx`），差异仅在后端路由层与风控/审计/熔断中间件。测试不操作真实资金，所有券商通路使用 Mock Adapter。

- **Total E2E Scenarios**: 14
- **Total UAT Scenarios**: 8
- **AC Coverage**: AC-11.1 ~ AC-11.9（全部 9 条验收条件）
- **Status**: 等待 code-review 通过

---

## AC 覆盖矩阵（PRD SS3.11 原文引用）

| AC | 描述 | E2E 场景 | UAT 场景 |
|----|------|----------|----------|
| AC-11.1 | 实盘交易界面与模拟交易一致（同花顺级别易用性） | E2E-1, E2E-2 | UAT-1, UAT-6 |
| AC-11.2 | 支持接入券商账号（短期：xtquant/QMT/同花顺） | E2E-8 | UAT-1 |
| AC-11.3 | 实盘下单前强制风控检查：资金/持仓/涨跌停/仓位上限/单笔上限 | E2E-3, E2E-4, E2E-5 | UAT-2 |
| AC-11.4 | 大额交易需二次确认（阈值可通过风控平台配置，默认 50 万） | E2E-6 | UAT-3 |
| AC-11.5 | 量化实盘交易：从确认方案自动生成量化策略（全自动/半自动可选） | E2E-11 | UAT-1 |
| AC-11.6 | 用户可自定义量化交易策略：买入/卖出/仓位/风控规则 | E2E-12 | UAT-1 |
| AC-11.7 | 所有实盘操作完整审计日志（不可删除） | E2E-9 | UAT-5 |
| AC-11.8 | 日内熔断机制：当日亏损超过阈值自动暂停交易 | E2E-10 | UAT-4 |
| AC-11.9 | 实盘和模拟盘的界面和操作逻辑完全一致，仅资金是真是假的区别 | E2E-1 | UAT-6 |

---

## Pre-conditions Checked

> 执行测试前必须全部勾选。

- [ ] 单元测试 + lint + typecheck 全绿
- [ ] code-reviewer 报告已存在且 verdict != Block（含 SIT Audit = OK / WARN）
- [ ] PRD SS3.11 可访问
- [ ] 测试数据库已启动（`docker compose up -d`）
- [ ] 数据库迁移已 apply（orders / positions / audit_logs / risk_config / circuit_breaker 表存在）
- [ ] 测试 seed 数据已注入（测试用户 + 模拟账户 + 风控默认配置 + 熔断默认阈值）
- [ ] 后端服务已启动（FastAPI on localhost:8000）
- [ ] 前端服务已启动（React on localhost:5173）
- [ ] Mock Broker Gateway 已启动（模拟券商连接/断开/回报）
- [ ] chrome-devtools-mcp 可用于浏览器截图

---

## 测试环境准备

### 3.1 测试用户预设（Seed Data）

```sql
-- 测试用户（密码均为 Test@123456，bcrypt hash）
INSERT INTO users (id, username, email, password_hash, role, is_active, created_at) VALUES
  ('u-admin',      'test_admin',    'admin@test.local',   '$2b$12$deadbeef', 'admin', true, NOW()),
  ('u-user-a',     'test_user_a',   'usera@test.local',   '$2b$12$deadbeef', 'user',  true, NOW()),
  ('u-user-b',     'test_user_b',   'userb@test.local',   '$2b$12$deadbeef', 'user',  true, NOW());
```

### 3.2 模拟账户 Seed

```sql
-- 模拟账户（用于实盘模式测试）
INSERT INTO live_accounts (id, user_id, broker, broker_account_id, total_capital, available, frozen, created_at) VALUES
  ('la-a-xt',   'u-user-a', 'xtquant',  'xt-888888', 1000000.00, 1000000.00, 0.00, NOW()),
  ('la-b-qmt',  'u-user-b', 'qmt',      'qmt-999999', 5000000.00, 5000000.00, 0.00, NOW());
```

### 3.3 风控默认配置 Seed

```sql
INSERT INTO risk_config (id, key, value, value_type, description, updated_at) VALUES
  ('rc-1', 'max_position_ratio',     '0.3',  'float',  '单票仓位上限（占总资产比）', NOW()),
  ('rc-2', 'max_single_order_amount','500000','int',    '单笔委托金额上限（元）',    NOW()),
  ('rc-3', 'large_order_threshold',  '500000','int',    '大额二次确认阈值（元）',    NOW()),
  ('rc-4', 'daily_loss_limit_ratio', '0.05',  'float',  '日内亏损熔断阈值（占总资产比）', NOW()),
  ('rc-5', 'circuit_breaker_cooldown','3600', 'int',    '熔断冷却时间（秒）',         NOW()),
  ('rc-6', 'limit_up_down_check',    'true',  'bool',   '涨跌停校验开关',              NOW()),
  ('rc-7', 'require_funds_check',    'true',  'bool',   '资金充足校验开关',             NOW());
```

### 3.4 Mock Broker Gateway 预设

```
Mock Broker Gateway 端点：
  POST /mock/xtquant/connect     -- 模拟券商连接
  POST /mock/xtquant/disconnect  -- 模拟券商断开
  POST /mock/xtquant/order       -- 模拟下单回报
  POST /mock/xtquant/cancel      -- 模拟撤单
  POST /mock/xtquant/positions   -- 模拟持仓同步
  GET  /mock/xtquant/status      -- 查询连接状态
  POST /mock/xtquant/reconnect   -- 模拟断线重连
```

### 3.5 测试前置条件 Checklist

| 序号 | 条件 | 验证方式 |
|------|------|---------|
| P1 | 用户 u-user-a 已登录 | 浏览器 Cookie 存在 `access_token` |
| P2 | 券商 xtquant 已绑定到 u-user-a | GET /api/v1/broker/bindings 返回 xtquant |
| P3 | 实盘账户 la-a-xt 可用资金 100 万 | GET /api/v1/trade/account?mode=live |
| P4 | Mock Broker Gateway 可达 | curl http://localhost:8001/mock/xtquant/status |
| P5 | 风控配置已加载到 Redis/内存 | GET /api/v1/admin/risk-config |

---

## Part 1: E2E 测试场景（14 条）

> 执行方式：chrome-devtools-mcp 浏览器自动化 + API 直接调用混合。每条场景必须截图关键步骤并附到执行报告中。

---

### E2E-1: 模拟/实盘模式切换 -- UI 一致性验证

| 项 | 内容 |
|----|------|
| **AC 覆盖** | AC-11.1, AC-11.9 |
| **前置** | P1（用户已登录） |
| **步骤** | 1. 导航至 `/trade` 页面<br>2. 确认默认模式为「模拟交易」<br>3. 截图：模拟模式下的下单面板、持仓表、委托记录、KPI Banner<br>4. 点击模式切换开关，切换至「实盘交易」<br>5. 确认页面顶部 Tag 变为红色「实盘交易」<br>6. 截图：实盘模式下的下单面板、持仓表、委托记录、KPI Banner<br>7. 对比步骤 3 和步骤 6 的截图，验证布局完全一致 |
| **断言** | - 模拟/实盘切换后 UI 布局 100% 一致（Tag 颜色除外）<br>- 下单面板字段相同（代码/方向/价格/数量）<br>- 切换操作 < 500ms 内完成 UI 刷新<br>- 模式参数正确传递到 API（请求头或 query 参数含 `mode=paper\|live`） |
| **风险** | 切换时数据未刷新，残留上一模式的数据 |

---

### E2E-2: 实盘下单 -- 风控通过 -- 成交

| 项 | 内容 |
|----|------|
| **AC 覆盖** | AC-11.1, AC-11.3 |
| **前置** | P1~P5 全部满足 |
| **步骤** | 1. 切换至实盘模式<br>2. 在「股票代码」输入 `000001`<br>3. 方向选择「买入」<br>4. 价格输入 `12.50`<br>5. 数量输入 `1000`（金额 = 12,500，低于大额阈值）<br>6. 点击「下单」按钮<br>7. 观察风控检查通过（无拦截弹窗）<br>8. 观察 Mock Broker 返回成交确认<br>9. 验证委托记录新增一条 status=FILLED 的记录<br>10. 验证可用资金减少 `12,500 + 手续费`<br>11. 验证持仓新增 `000001` 股票 1000 股 |
| **断言** | - 下单成功，HTTP 200<br>- 风控检查日志包含：资金校验 OK / 持仓校验 OK / 涨跌停校验 OK / 仓位上限 OK / 单笔上限 OK<br>- 委托记录 status = FILLED<br>- 可用资金 = 原可用资金 - 成交金额 - 手续费<br>- 持仓增加 1000 股 |
| **风险** | Mock Broker 回报延迟导致前端状态不一致 |

---

### E2E-3: 实盘下单 -- 资金不足 -- 拦截

| 项 | 内容 |
|----|------|
| **AC 覆盖** | AC-11.3（资金校验） |
| **前置** | P1~P5，账户可用资金 100 万 |
| **步骤** | 1. 实盘模式下，下单买入 `000001`，价格 `200.00`，数量 `100000`（金额 = 2000 万 > 可用资金 100 万）<br>2. 点击「下单」<br>3. 观察前端弹窗提示「资金不足」<br>4. 确认订单未提交到 Mock Broker<br>5. 确认委托记录无新增 |
| **断言** | - HTTP 400 或 422，错误码 `INSUFFICIENT_FUNDS`<br>- 前端展示明确中文提示 `可用资金不足，当前可用 ¥1,000,000，需要 ¥20,000,000`<br>- 审计日志记录一条风控拦截事件（action_type=risk_block）<br>- Mock Broker 未收到订单请求 |
| **风险** | 错误提示模糊，用户不知道差多少钱 |

---

### E2E-4: 实盘下单 -- 超仓位上限 -- 拦截

| 项 | 内容 |
|----|------|
| **AC 覆盖** | AC-11.3（仓位上限校验） |
| **前置** | P1~P5，max_position_ratio = 0.3（即单票最多占总资产 30 万） |
| **步骤** | 1. 先下单买入 `000001` 2000 股（约 25 万，接近上限）<br>2. 再下单买入 `000001` 1000 股（约 12.5 万，合计 37.5 万 > 30 万上限）<br>3. 观察前端弹窗提示「超仓位上限」<br>4. 确认第二次订单未提交 |
| **断言** | - HTTP 400，错误码 `POSITION_LIMIT_EXCEEDED`<br>- 前端展示 `单票仓位超限：当前 000001 持仓占比 25%，再买入 1000 股后将达到 37.5%，超过上限 30%`<br>- 审计日志记录风控拦截<br>- 第一次订单不受影响 |
| **风险** | 同时多笔并发下单时仓位校验存在竞态条件 |

---

### E2E-5: 实盘下单 -- 涨跌停校验 -- 拦截

| 项 | 内容 |
|----|------|
| **AC 覆盖** | AC-11.3（涨跌停校验） |
| **前置** | P1~P5，Mock Broker 返回 `000001` 处于涨停状态 |
| **步骤** | 1. Mock Broker 配置：`000001` 当日涨停（buyable=false）<br>2. 实盘模式下，下单买入 `000001` 1000 股<br>3. 观察前端弹窗提示「该股票已涨停，无法买入」<br>4. 切换方向为「卖出」<br>5. 下单卖出 `000001`（假设有持仓），应通过 |
| **断言** | - 买入时 HTTP 400，错误码 `LIMIT_UP_BLOCKED`<br>- 前端展示 `000001 已涨停，暂停买入`<br>- 卖出方向不受涨跌停影响<br>- 审计日志记录拦截事件 |
| **风险** | 涨跌停数据延迟导致误判 |

---

### E2E-6: 大额交易 -- 二次确认弹窗

| 项 | 内容 |
|----|------|
| **AC 覆盖** | AC-11.4 |
| **前置** | P1~P5，large_order_threshold = 500,000 |
| **步骤** | 1. 实盘模式下，下单买入 `000001`，价格 `100.00`，数量 `6000`（金额 = 600,000 > 500,000 阈值）<br>2. 点击「下单」<br>3. 观察弹出二次确认弹窗：展示订单摘要（代码/方向/价格/数量/金额）<br>4. 弹窗显示「确认」和「取消」按钮<br>5. 点击「取消」：弹窗关闭，订单不提交<br>6. 再次下单，点击「确认」：订单提交<br>7. 验证委托记录新增一条 |
| **断言** | - 金额 > 500,000 时弹出二次确认<br>- 弹窗内容包含：股票代码、方向、价格、数量、估算金额、手续费<br>- 取消 → 无订单创建<br>- 确认 → 订单正常提交<br>- 金额 < 500,000 时无二次确认 |
| **风险** | 确认弹窗 UI 在移动端/小屏下布局错乱 |

---

### E2E-7: 撤单流程（实盘）

| 项 | 内容 |
|----|------|
| **AC 覆盖** | AC-11.1（交易基本操作） |
| **前置** | P1~P5，存在一笔 status=PENDING 的限价委托 |
| **步骤** | 1. 实盘模式下，下单限价买入 `000001`，价格 `10.00`，数量 `1000`（价格远低于市价，不会立即成交）<br>2. 委托记录出现一条 status=PENDING 的记录<br>3. 点击该记录的「撤单」按钮<br>4. 观察撤单确认提示<br>5. 确认撤单<br>6. 委托记录 status 变为 CANCELLED<br>7. 可用资金恢复（解冻的预扣资金） |
| **断言** | - 撤单成功，HTTP 200<br>- 委托记录 status 从 PENDING → CANCELLED<br>- 被冻结的资金回到可用资金<br>- Mock Broker 收到撤单请求<br>- 审计日志记录撤单事件 |
| **风险** | 撤单与成交的竞态：Mock Broker 在撤单前回报成交 |

---

### E2E-8: 券商连接 / 断开 / 重连

| 项 | 内容 |
|----|------|
| **AC 覆盖** | AC-11.2 |
| **前置** | P1，Mock Broker Gateway 启动 |
| **步骤** | 1. 切换至实盘模式<br>2. 页面显示券商连接状态「已连接」（绿色）<br>3. Mock Broker：模拟断开连接 `POST /mock/xtquant/disconnect`<br>4. 前端状态变为「已断开」（红色），下单按钮禁用<br>5. Mock Broker：模拟重连 `POST /mock/xtquant/reconnect`<br>6. 前端状态恢复「已连接」，下单按钮恢复可用<br>7. Mock Broker：模拟行情推送中断 30 秒后自动恢复<br>8. 验证自动重连机制触发 |
| **断言** | - 连接状态实时展示（WebSocket 或轮询 < 3s 延迟）<br>- 断开时下单按钮 disabled，且 tooltip 提示「券商已断开」<br>- 重连后自动恢复可用<br>- 断线期间收到的委托请求返回 HTTP 503 `BROKER_DISCONNECTED`<br>- 审计日志记录连接/断开/重连事件 |
| **风险** | 重连后持仓数据不一致 |

---

### E2E-9: 审计日志写入 + 不可删除验证

| 项 | 内容 |
|----|------|
| **AC 覆盖** | AC-11.7 |
| **前置** | P1~P5，已执行至少 3 笔实盘操作（下单成功/风控拦截/撤单各一） |
| **步骤** | 1. 管理员登录，导航至「审计日志」页面<br>2. 筛选：用户=test_user_a、日期=今天、类型=全部<br>3. 验证 3 条记录均存在<br>4. 尝试删除任一条记录（API：DELETE /api/v1/admin/audit-logs/{id}）<br>5. 尝试修改任一条记录（API：PATCH /api/v1/admin/audit-logs/{id}）<br>6. 尝试通过数据库直连删除（需有 DB 权限的测试） |
| **断言** | - 审计日志包含字段：id / user_id / action_type / target / detail / ip / user_agent / created_at<br>- DELETE API 返回 405 Method Not Allowed 或 403 Forbidden<br>- PATCH API 返回 405 Method Not Allowed 或 403 Forbidden<br>- 数据库层面：audit_logs 表仅授予 SELECT 权限（无 DELETE/UPDATE）<br>- 日志按时间倒序排列 |
| **风险** | 数据库权限配置错误，允许直接 DELETE |

---

### E2E-10: 熔断触发（模拟超阈值）

| 项 | 内容 |
|----|------|
| **AC 覆盖** | AC-11.8 |
| **前置** | P1~P5，daily_loss_limit_ratio = 0.05（日内亏损上限 = 50,000），Mock Broker 可注入亏损 |
| **步骤** | 1. 当日已实现亏损 30,000（通过 mock 注入 3 笔亏损交易）<br>2. 账户总资产 1,000,000，当前亏损比例 3%，未触发熔断<br>3. 再通过 mock 注入一笔亏损 25,000 的交易（累计亏损 55,000 > 50,000）<br>4. 亏损比例达到 5.5% > 5% 阈值<br>5. 系统自动触发熔断<br>6. 前端显示熔断状态 Banner：`日内亏损已达 ¥55,000 (5.5%)，交易已暂停`<br>7. 下单按钮禁用，显示「熔断中」<br>8. 尝试 API 下单，返回 HTTP 403 `CIRCUIT_BREAKER_TRIGGERED`<br>9. 管理员可在熔断冷却时间后手动解除（或配置自动解除） |
| **断言** | - 亏损超阈值后 5 秒内触发熔断<br>- 前端醒目显示熔断状态（红色 Banner + 禁用下单）<br>- API 下单返回 `CIRCUIT_BREAKER_TRIGGERED`<br>- 审计日志记录熔断触发事件（含触发时亏损金额和比例）<br>- 熔断不影响模拟交易（仅锁定实盘） |
| **风险** | 熔断误触发：计算亏损时错误包含浮盈 |

---

### E2E-11: 量化实盘交易 -- 从确认方案生成策略并执行（半自动模式）

| 项 | 内容 |
|----|------|
| **AC 覆盖** | AC-11.5 |
| **前置** | P1~P5，存在一份已确认的方案（含买入/卖出条件） |
| **步骤** | 1. 导航至「方案管理」页面<br>2. 选择一份已确认的方案<br>3. 点击「生成量化策略」→ 选择「实盘」→ 模式选「半自动确认」<br>4. 系统自动生成策略（买入条件/卖出条件/仓位规则/风控规则）<br>5. 策略进入「待激活」状态<br>6. 激活策略<br>7. 当买入条件触发时，前端弹出确认对话框（而非自动下单）<br>8. 用户确认后，订单提交到实盘<br>9. 当卖出条件触发时，同样弹出确认 |
| **断言** | - 策略生成成功，包含完整的买入/卖出/仓位/风控规则<br>- 半自动模式下，每次触发都需用户确认<br>- 确认弹窗含策略名称、触发条件、建议订单详情<br>- 用户拒绝后不提交订单<br>- 策略可随时暂停/恢复/终止 |
| **风险** | 条件触发与用户确认之间的时间窗口导致价格偏离 |

---

### E2E-12: 用户自定义量化策略 -- 配置规则 + 生效

| 项 | 内容 |
|----|------|
| **AC 覆盖** | AC-11.6 |
| **前置** | P1~P5 |
| **步骤** | 1. 导航至「策略管理」→「新建自定义策略」<br>2. 配置买入条件：`MA5 上穿 MA20` + `成交量 > 1.5 倍均量`<br>3. 配置卖出条件：`止盈 10%` + `止损 5%` + `MA5 下穿 MA20`<br>4. 配置仓位规则：`每次买入 20% 仓位` + `最大持仓 5 只`<br>5. 配置风控规则：`单日最大亏损 3%` + `连续止损 3 次暂停`<br>6. 选择标的池 `["000001", "000002", "600000"]`<br>7. 选择模式「半自动确认」<br>8. 保存并激活<br>9. 验证策略已出现在「运行中策略」列表<br>10. Mock 触发买入条件，验证确认弹窗 |
| **断言** | - 策略创建成功，所有规则持久化存储<br>- 策略列表显示策略名称/状态/标的池/创建时间<br>- 条件触发时正确弹出确认<br>- 策略编辑后规则更新<br>- 策略删除后不再触发 |
| **风险** | 复杂条件组合的 AND/OR 逻辑错误 |

---

### E2E-13: 实盘持仓与券商同步

| 项 | 内容 |
|----|------|
| **AC 覆盖** | AC-11.2（券商接入延伸） |
| **前置** | P1~P5，Mock Broker 持有不同步的持仓数据 |
| **步骤** | 1. 系统持仓：000001 = 1000 股<br>2. Mock Broker 持仓：000001 = 1500 股（外部买入 500 股）<br>3. 手动触发「同步持仓」<br>4. 系统检测到差异<br>5. 前端展示差异列表：`000001 系统:1000 / 券商:1500 (+500)`<br>6. 用户确认同步<br>7. 系统持仓更新为 1500 股<br>8. 审计日志记录持仓同步事件 |
| **断言** | - 同步功能检测并展示所有差异<br>- 用户可以逐条确认或一键全选<br>- 同步后系统持仓与券商一致<br>- 差异调整自动生成调账记录 |
| **风险** | 频繁同步覆盖用户手动持仓调整 |

---

### E2E-14: 并发下单 -- 风控竞态保护

| 项 | 内容 |
|----|------|
| **AC 覆盖** | AC-11.3（风控完整性） |
| **前置** | P1~P5，账户可用资金 100 万 |
| **步骤** | 1. 同时发起 5 笔买入 `000001` 请求，每笔金额 30 万（合计 150 万 > 可用资金 100 万）<br>2. 使用 Promise.all 或 curl 并发发送<br>3. 验证只有部分订单成功（总成交金额 <= 100 万）<br>4. 验证没有超额成交 |
| **断言** | - 风控检查使用数据库行锁或 Redis 分布式锁<br>- 并发请求中，总成交金额不超过可用资金<br>- 被拒绝的请求返回 `INSUFFICIENT_FUNDS`<br>- 无死锁或超时 |
| **风险** | 锁粒度不足导致超额成交 |

---

## Part 2: UAT 测试场景（8 条）

> 执行方式：真实浏览器手动操作 + 业务方（rogerluo2016ROL）验收。每条 UAT 需要有业务方签字确认。

---

### UAT-1: 完整买卖闭环（模拟同花顺体验）

| 项 | 内容 |
|----|------|
| **AC 覆盖** | AC-11.1, AC-11.2, AC-11.5, AC-11.6 |
| **角色** | 普通用户（test_user_a） |
| **业务场景** | 用户从选股到卖出完成完整交易闭环，体验对标同花顺 |
| **步骤** | 1. 登录系统<br>2. 从选股结果中选择一只股票 `000001`<br>3. 查看该股票的 K 线图、F10 信息、AI 预测<br>4. 进入「交易中心」，切换至实盘模式<br>5. 以市价买入 1000 股<br>6. 观察成交回报（含手续费明细）<br>7. 查看持仓变化（新增 `000001`）<br>8. 持仓页面显示浮动盈亏实时更新<br>9. 设置止盈止损条件单<br>10. 当股价达到止盈价时，卖出订单触发（半自动确认）<br>11. 确认卖出<br>12. 查看交易统计（本次买卖盈亏）<br>13. 验证审计日志完整记录 |
| **签字点** | - 买卖流程操作流畅度是否满意？<br>- 成交回报速度是否可接受（< 2 秒）？<br>- 盈亏计算是否准确（含手续费）？<br>- 是否感觉与同花顺操作体验接近？ |
| **签字** | _（业务方在此签字）_______________ 日期: _______ |

---

### UAT-2: 风控规则配置 + 生效验证

| 项 | 内容 |
|----|------|
| **AC 覆盖** | AC-11.3 |
| **角色** | 管理员（test_admin） |
| **业务场景** | 管理员调整风控参数，验证新规则立即生效 |
| **步骤** | 1. 管理员登录，导航至「风控配置」<br>2. 当前 max_single_order_amount = 500,000<br>3. 修改为 max_single_order_amount = 200,000<br>4. 保存配置<br>5. 普通用户尝试下单金额 300,000<br>6. 验证被拦截：`单笔委托超限：300,000 > 200,000`<br>7. 管理员将阈值改回 500,000<br>8. 普通用户重新下单 300,000<br>9. 验证通过 |
| **签字点** | - 风控配置修改后是否立即生效（< 5 秒）？<br>- 配置变更是否有审计日志？<br>- 配置界面是否直观易用？ |
| **签字** | _（业务方在此签字）_______________ 日期: _______ |

---

### UAT-3: 大额确认阈值可配置

| 项 | 内容 |
|----|------|
| **AC 覆盖** | AC-11.4 |
| **角色** | 管理员（test_admin） + 普通用户（test_user_a） |
| **业务场景** | 管理员可根据需要调整大额确认阈值 |
| **步骤** | 1. 管理员登录 → 风控配置<br>2. 默认 large_order_threshold = 500,000<br>3. 普通用户下单 400,000：无二次确认<br>4. 管理员修改阈值 = 300,000<br>5. 普通用户下单 400,000：弹出二次确认<br>6. 管理员修改阈值 = 1,000,000<br>7. 普通用户下单 400,000：无二次确认 |
| **签字点** | - 阈值配置支持任意金额（正实数）？<br>- 阈值变更后是否需要用户重新登录？<br>- 阈值设为 0 是否表示禁用二次确认？ |
| **签字** | _（业务方在此签字）_______________ 日期: _______ |

---

### UAT-4: 熔断后恢复流程

| 项 | 内容 |
|----|------|
| **AC 覆盖** | AC-11.8 |
| **角色** | 管理员（test_admin） + 普通用户（test_user_a） |
| **业务场景** | 熔断触发后，管理员确认风险后手动解除熔断 |
| **步骤** | 1. 触发熔断（模拟日内亏损超阈值）<br>2. 普通用户无法下单，看到熔断 Banner<br>3. 管理员收到熔断通知（系统通知 + 可选邮件/短信）<br>4. 管理员登录 → 风控面板 → 熔断管理<br>5. 查看熔断详情：触发时间、累计亏损、触发阈值、受影响用户<br>6. 管理员评估风险，点击「解除熔断」<br>7. 需输入解除原因（必填）<br>8. 普通用户恢复交易权限<br>9. 审计日志记录：熔断解除时间、解除人、解除原因 |
| **签字点** | - 熔断通知是否及时（< 30 秒）？<br>- 解除流程是否有足够的审批控制？<br>- 冷却时间内是否允许强制解除？<br>- 解除后是否可以追溯熔断原因？ |
| **签字** | _（业务方在此签字）_______________ 日期: _______ |

---

### UAT-5: 审计日志完整性审查

| 项 | 内容 |
|----|------|
| **AC 覆盖** | AC-11.7 |
| **角色** | 管理员（test_admin） |
| **业务场景** | 对指定时间段的实盘操作进行完整审计追踪 |
| **步骤** | 1. 管理员登录 → 审计日志<br>2. 筛选条件：日期范围（当天）、类型=全部、用户=全部<br>3. 导出完整审计日志为 CSV<br>4. 逐条审查以下类型是否全部记录：<br>  - 用户登录<br>  - 模式切换（模拟↔实盘）<br>  - 下单请求（含风控检查详情）<br>  - 风控拦截<br>  - 大额二次确认<br>  - 成交回报<br>  - 撤单<br>  - 券商连接/断开/重连<br>  - 熔断触发/解除<br>  - 持仓同步<br>  - 风控配置变更<br>5. 验证每条日志的 ip / user_agent / timestamp 不可为空<br>6. 验证无日志缺失 |
| **签字点** | - 审计日志是否包含所有实盘操作类型？<br>- 日志字段是否完整？<br>- CSV 导出是否完整可读？<br>- 日志查询性能是否可接受（10 万条内 < 3 秒）？ |
| **签字** | _（业务方在此签字）_______________ 日期: _______ |

---

### UAT-6: 模拟 ↔ 实盘切换 -- 无数据混淆

| 项 | 内容 |
|----|------|
| **AC 覆盖** | AC-11.9 |
| **角色** | 普通用户（test_user_a） |
| **业务场景** | 用户在模拟和实盘间频繁切换，确保数据隔离 |
| **步骤** | 1. 在模拟模式下，买入 `000001` 1000 股<br>2. 记下模拟账户总资产、持仓<br>3. 切换至实盘模式<br>4. 验证实盘持仓中不含 `000001`（模拟盘买入的）<br>5. 验证实盘总资产是独立的<br>6. 在实盘模式下，买入 `600000` 500 股<br>7. 切换回模拟模式<br>8. 验证模拟持仓中不含 `600000`（实盘买入的）<br>9. 验证模拟账户总资产未受实盘交易影响<br>10. 验证委托记录按模式过滤（模拟页只显示模拟委托） |
| **断言** | - 模拟和实盘的账户/持仓/委托/盈亏完全隔离<br>- 模式切换不清空数据，各自独立持久化<br>- 前端切换时不闪现另一模式的数据<br>- API 层面 mode=paper / mode=live 返回各自数据 |
| **签字点** | - 切换时是否有任何数据混淆？<br>- 模拟盘盈亏是否影响实盘熔断计算？<br>- 是否容易误操作在实盘模式下下单？ |
| **签字** | _（业务方在此签字）_______________ 日期: _______ |

---

### UAT-7: 极端行情下的风控压力测试

| 项 | 内容 |
|----|------|
| **AC 覆盖** | AC-11.3, AC-11.8 |
| **角色** | 管理员（test_admin） + 普通用户（test_user_a） |
| **业务场景** | 模拟大盘暴跌场景，验证风控和熔断在高负载下正常工作 |
| **步骤** | 1. Mock Broker 配置：全市场 80% 股票跌停<br>2. 用户尝试买入跌停股票 → 拦截（涨跌停校验）<br>3. 用户持仓股票大幅亏损<br>4. 熔断触发<br>5. 用户尝试卖出 → 因熔断被拦截<br>6. 管理员紧急评估：是否解除熔断允许止损卖出<br>7. 管理员临时配置：熔断期间允许卖出、禁止买入<br>8. 用户卖出成功<br>9. 买入仍被拦截 |
| **签字点** | - 熔断后是否应该允许止损卖出？<br>- 涨跌停数据是否准确及时？<br>- 极端行情下系统响应是否正常（< 3 秒）？ |
| **签字** | _（业务方在此签字）_______________ 日期: _______ |

---

### UAT-8: 多用户并发实盘交易

| 项 | 内容 |
|----|------|
| **AC 覆盖** | AC-11.3, AC-11.7 |
| **角色** | 多个普通用户（test_user_a + test_user_b） |
| **业务场景** | 多个用户同时进行实盘交易，验证系统隔离和性能 |
| **步骤** | 1. 用户 A 和用户 B 同时登录<br>2. 用户 A 买入 `000001` 500 股<br>3. 用户 B 买入 `600000` 1000 股<br>4. 验证两个用户的账户/持仓/委托互不影响<br>5. 管理员查看审计日志：按用户分组显示<br>6. 验证交易延迟 < 500ms（模拟环境） |
| **签字点** | - 多用户数据是否完全隔离？<br>- 并发交易是否有性能下降？<br>- 审计日志是否能按用户清晰分组？ |
| **签字** | _（业务方在此签字）_______________ 日期: _______ |

---

## 测试数据准备脚本

```sql
-- ============================================
-- 实盘交易 E2E/UAT 测试 Seed 数据
-- ============================================

-- 1. 清理旧数据（仅测试环境执行）
-- DELETE FROM audit_logs WHERE user_id IN ('u-user-a', 'u-user-b');
-- DELETE FROM orders WHERE account_id IN ('la-a-xt', 'la-b-qmt');
-- DELETE FROM positions WHERE account_id IN ('la-a-xt', 'la-b-qmt');
-- DELETE FROM circuit_breaker_state WHERE account_id IN ('la-a-xt', 'la-b-qmt');

-- 2. 风控默认配置
INSERT INTO risk_config (id, key, value, value_type, description) VALUES
  ('rc-1', 'max_position_ratio',      '0.3',    'float', '单票仓位上限（占总资产比）'),
  ('rc-2', 'max_single_order_amount', '500000',  'int',   '单笔委托金额上限（元）'),
  ('rc-3', 'large_order_threshold',   '500000',  'int',   '大额二次确认阈值（元）'),
  ('rc-4', 'daily_loss_limit_ratio',  '0.05',   'float', '日内亏损熔断阈值（占总资产比）'),
  ('rc-5', 'circuit_breaker_cooldown','3600',   'int',   '熔断冷却时间（秒）'),
  ('rc-6', 'limit_up_down_check',     'true',   'bool',  '涨跌停校验开关'),
  ('rc-7', 'require_funds_check',     'true',   'bool',  '资金充足校验开关')
ON CONFLICT (key) DO UPDATE SET value = EXCLUDED.value;

-- 3. 测试股票基准数据（Mock Broker 使用）
INSERT INTO mock_stocks (code, name, last_price, limit_up, limit_down, buyable, sellable) VALUES
  ('000001', '平安银行', 12.50,  13.75,  11.25,  true,  true),
  ('000002', '万科A',    8.30,   9.13,   7.47,   true,  true),
  ('600000', '浦发银行', 10.20,  11.22,  9.18,   true,  true),
  ('600519', '贵州茅台', 1680.00,1848.00,1512.00,true,  true);
```

---

## Mock Broker API 速查表

| 端点 | 方法 | 用途 | 关键参数 |
|------|------|------|---------|
| `/mock/{broker}/connect` | POST | 模拟连接 | `account_id` |
| `/mock/{broker}/disconnect` | POST | 模拟断连 | `reason` |
| `/mock/{broker}/reconnect` | POST | 模拟重连 | -- |
| `/mock/{broker}/status` | GET | 查询状态 | -- |
| `/mock/{broker}/order` | POST | 提交模拟订单 | `code, direction, price, volume` |
| `/mock/{broker}/cancel/{order_id}` | POST | 模拟撤单 | -- |
| `/mock/{broker}/positions` | GET | 查询券商持仓 | -- |
| `/mock/{broker}/positions` | POST | 注入持仓差异 | `code, qty` |
| `/mock/{broker}/inject-loss` | POST | 注入亏损（熔断测试）| `amount` |
| `/mock/{broker}/set-stock-status` | POST | 设置涨跌停状态 | `code, is_limit_up, is_limit_down` |

---

## 风险与注意事项

| 序号 | 风险 | 影响 | 缓解措施 |
|------|------|------|---------|
| R1 | Mock Broker 行为与真实券商不一致 | 测试通过后生产仍有问题 | 在 UAT 阶段至少用 xtquant 模拟环境验证一次 |
| R2 | 风控配置修改后缓存未刷新 | 新规则不生效 | E2E 中增加配置变更后等待 + 验证步骤 |
| R3 | 数据库迁移未覆盖所有新表 | 测试无法进行 | Pre-conditions 中显式检查表存在性 |
| R4 | 前端模式切换状态管理混乱 | 实盘/模拟数据混淆 | E2E-1 和 UAT-6 双重覆盖 |
| R5 | 熔断阈值计算口径不一致 | 误触发或漏触发 | UAT-7 极端行情测试覆盖 |

---

## 测试执行顺序建议

```
Phase 1: 环境准备 + 冒烟
  E2E-1 (模式切换) → E2E-2 (正常下单) → E2E-8 (券商连接)

Phase 2: 风控核心
  E2E-3 (资金不足) → E2E-4 (仓位上限) → E2E-5 (涨跌停) → E2E-6 (大额确认) → E2E-14 (竞态)

Phase 3: 交易全流程
  E2E-7 (撤单) → E2E-9 (审计日志) → E2E-10 (熔断) → E2E-13 (持仓同步)

Phase 4: 量化策略
  E2E-11 (方案生成策略) → E2E-12 (自定义策略)

Phase 5: UAT（业务方参与）
  UAT-1 → UAT-2 → UAT-3 → UAT-4 → UAT-5 → UAT-6 → UAT-7 → UAT-8
```

---

## 签字栏

| 角色 | 姓名 | 签字 | 日期 |
|------|------|------|------|
| QA Engineer | -- | | |
| Product Lead | rogerluo2016ROL | | |
| Business Owner | rogerluo2016ROL | | |

---

> **状态**: 等待 code-review 通过后执行
> **下一步**: code-reviewer 输出报告 → 更新 Pre-conditions → 按执行顺序依次跑 E2E → E2E 全绿后跑 UAT
