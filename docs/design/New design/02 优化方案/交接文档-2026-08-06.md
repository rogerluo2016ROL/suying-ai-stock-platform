# 速赢AI 前后端优化交接文档

> 日期：2026-08-06
> 范围：基于 `docs/design/New design/01 PRD 文档/` 的全量 PRD/详细设计/HTML 预览，完成三大任务：HTML 预览优化、后端设计文档收口、前后端代码优化。
> 方案文档：`docs/design/New design/02 优化方案/前后端优化建议-2026-08-06.md`（含执行日志）

---

## 一、任务总览

| 任务 | 内容 | 状态 |
|---|---|---|
| 任务一 | HTML 预览优化（视觉/token/bug/交互/跨页状态） | ✅ |
| 任务二 | 后端设计文档收口（契约/角色/安全/勘误/缺口） | ✅ |
| 任务三 | 前后端代码优化（C1安全 + C2拆分 + C3深化 + D端点） | ✅ |

---

## 二、任务一：HTML 预览优化

### 共享资产改造（`01 PRD 文档/assets/`）

| 文件 | 改动 |
|---|---|
| `shell.js` | 默认暗色主题（localStorage 持久化）；统一注入空 favicon；页签导航移除已合并页面（6.3/11.0/15.1） |
| `preview-interactions.js` | toast 'success'→'ok' 归一；新增 `suyingConfirm` 共享确认弹窗（Esc/遮罩关闭、danger 变体）；新增 `suyingEchartsTheme()`；staticAction 排除 `.dlg-mask` 内按钮 |
| `preview-adapter.css` | 新增栅格 `.r-7-5/.r-16-8/.r-3-2`、`.dlg-*` 弹窗样式、`.toast.success`；platform-scope-strip 改 token |
| `app.css` | 新增 `.warn` 工具类 |
| `echarts.min.js` | 新增（5.5.0 本地化，27 页脱离 CDN） |

### 页面修复（40+ 文件）

- **P0 运行时 bug**：5 个缺失弹窗补 DOM（7.3 改单/7.5/8.1 跟单/8.2/14.0）、1.3/2.3/3.1 每秒抛错修复、7.4 ReferenceError、全局 event 依赖改显式传参
- **红绿语义修正** 10+ 处（10.0/10.3/12.4/1.4/7.4 等）
- **6.0 信号详情**正文补全（verdict 卡+六维雷达+评分明细+资金流+风险扫描）；6.3/11.0/15.1 改为重定向页
- **token 单源化**：全部页面删除内联 `:root` 死代码，AntD 色板归并 token
- **ECharts**：init 统一接 `suyingEchartsTheme()`；option 内 `var(--)` 全清；12.0/12.3 hex→rgba bug 修
- **alert/confirm** 全站替换为 suyingToast/suyingConfirm

### 全量交互审计（200+ 处修复，570+ 断言 PASS）

- 筛选/排序/分页全部真实化；表单校验（空值/超可买/超可卖）；状态机闭环（暂停/恢复/结算/启停）
- 缺失功能按详设补齐：2.5 策略启停、3.3 因子联动、13.1 Drawer、14.0 注册 Modal、16.0 三角色视图

### 跨页 mock 状态统一

- `suying_mock_candidate_pool` / `suying_mock_watchlist` 统一 `[{code,name,src,date}]` schema（17 文件归一）
- 消费端闭环：2.4 渲染外部入池标的、1.4 合并外部自选、12.0 消费 `?code=`、7.1 消费 prefill

---

## 三、任务二：后端设计文档收口

### P0 安全违规修复

- 2.5 执行监控详设：删除前端注入 `X-User-Role` 设计，改为后端 JWT 解析角色执行

### API 契约收口（10 项，grep 验证零残留）

| 矛盾 | 收口 |
|---|---|
| 方案提交 generate-and-start/generate | `POST /api/v1/strategy/generate-from-picks` |
| 风控 check-batch 归属 | `POST /api/v1/trade/risk/check-batch`（trade-service） |
| 信号 analyze GET vs POST | `GET /api/v1/signal/analyze/{ts_code}` |
| 跟单 fork vs follow | `POST /api/v1/strategy/market/{id}/follow` |
| trades /today vs ?today=1 | 查询参数式 |
| 产业链 LLM 端点三种写法 | `/api/v1/screener/supply-chain/policy/analyze` |
| 回测明细 strategyId vs backtestId | backtestId |
| 清仓逐只 vs 批量 | 批量端点 |
| 暂停 /stop 表暂停 | `/pause` + `/resume` 拆分 |
| 策略广场卡片式 vs 清单式 | 清单式 |

### 缺口补设计

- 风控写操作 API 5 端点 + 审计表 schema + /market-regime schema + 评分统一口径（10.0 详设 §7）
- signal_history 表结构 + 回写任务 + 命中率 T+5（6.2 详设）
- MLflow mock/real 双模式界定（13.0 详设）
- 0.1 角色映射表 + 平台化字段注入规则；0.5 网关矩阵补登 8 前缀

### 文档勘误

- ECharts 5.5→6.1（40 文件批量）；4.x/13.x/14.x/15.x 编号漂移修正；7.0 子模块数、15.x 核心表数量、12.4 季度数、16.0 探测方式

---

## 四、任务三：前后端代码优化

### C1：安全 + 正确性 + 债务清理（439 tests PASS）

| 修复 | 文件 | 说明 |
|---|---|---|
| permission fail-open → fail-closed | `app/routeRegistry.tsx` | `buildMenuItems` 改为与 ProtectedRoute 一致的双层逻辑（permissions 加载时查 permission，未加载时仅查 role） |
| 删除双 HTTP 层死代码 | `api/core/http.ts` 删除 | `domainHttp` 全仓零引用 |
| trade API 合并 | `api/liveTrade.ts` 删除 → `api/domains/trade/api.ts` 扩展 | liveTradeApi 全部方法 + switchTradeMode 合并入 tradeApi，6 个消费文件 + 3 个测试文件同步迁移 |
| AutoTrade 裸调用 → strategyApi | `pages/AutoTrade.tsx` | strategyApi 新增 7 个执行器方法（listInstances/getInstance/getInstanceLog/start/pause/resume/stop），测试 mock 同步 |
| RiskControl 百分比双重归一化 bug | `pages/RiskControl.tsx` | overview Tab 统一使用 `percent()` 函数 |
| ModelRegistry 误归档风险 | `pages/ModelRegistry.tsx` | selectedModel fallback 从 `models[0]` 改为 `null`，归档按钮需显式选择 |

### C2：巨型组件拆分（4 个主文件均 <200 行）

| 文件 | 原行数 | 拆分后 | 子目录 |
|---|---|---|---|
| `Dashboard.tsx` | 1745 | 101 | `pages/dashboard/`（SentimentTab/AuctionTab/SignalsTab/WatchlistTab + helpers + types） |
| `OpenDecision.tsx` | 1419 | 152 | `pages/open-decision/`（OverviewTab/AuctionTab/SignalsTab/CandidatesTab/ExecutionTab + helpers + types） |
| `Screener.tsx` | 1330 | 170 | `pages/screener/`（WorkbenchTab/ModelsTab/FactorsTab + useModelCompare + helpers + types） |
| `SupplyChainBom.tsx` | 1362 | 198 | `pages/supply-chain-bom/`（复用已有 16 个子组件） |

### C3：页面深化

| 页面 | 行数 | 主要新增 |
|---|---|---|
| `RiskControl.tsx` | 264→558 | 熔断状态横幅、四层防御状态表、策略风控状态表、止损逼近告警、策略暂停/恢复按钮、市场情绪指标、审计日志表 |
| `AutoTrade.tsx` | 231→560 | 策略广场独立模板市场视图（筛选/排序/一键跟单）、config 参数编辑（PUT updateInstance）、monitor KPI+净值图、新增 logs Tab |
| `Training.tsx` | 244→516 | 发起训练 Modal（POST /training/run）、MLflow Loss/IC 曲线、「已完成」→「历史」、「进度」→「时长」 |
| `ModelRegistry.tsx` | 291→511 | 死 Tab 删除、「复核」→「查看生产版本」、版本对比雷达图、30 日趋势图、loading 骨架 |

### D：后端缺口端点（5 个服务，176 条测试全绿）

| 服务 | 文件 | 端点 |
|---|---|---|
| trade-service 8006 | `app/routers/risk.py` (577 行） | `GET /risk-dashboard`、`POST /risk/strategy/{id}/pause|resume`、`POST /risk/position/{code}/stop-loss|reduce`、`POST /risk/liquidate-all`、`POST /risk/check-batch` |
| strategy-service 8003 | `app/routes.py` + `app/settlement_store.py` | `GET /plans/{id}/settlement-report` |
| signal-service 8004 | `app/routers/sentiment.py` + `app/sentiment.py` | `GET /sentiment-index`、`GET /sentiment-history`、`GET /sentiment-alerts` |
| data-service 8010 | `app/routers/data.py` + `app/inventory.py` | `GET /data/tables/{table_id}/preview` |
| api-gateway 8080 | `app/runtime.py` | `GET /runtime/readiness` 扩展（12 服务+PG+Redis 状态矩阵） |

---

## 五、测试覆盖

| 范围 | 命令 | 结果 |
|---|---|---|
| 前端单元测试 | `cd frontend && npx vitest run` | **446/446 PASS**（64 文件） |
| 前端 TypeScript | `cd frontend && npx tsc -b --noEmit` | **零 error** |
| trade-service | `cd services/trade-service && pytest tests/ -v` | **64/64 PASS** |
| api-gateway | `cd services/api-gateway && pytest tests/ -v` | **24/24 PASS** |
| signal-service | `cd services/signal-service && pytest tests/ -v` | **36/36 PASS** |
| strategy-service | `cd services/strategy-service && pytest tests/ -v` | **18/18 PASS** |
| data-service | `cd services/data-service && pytest tests/ -v` | **54/54 PASS** |

---

## 六、遗留事项

### 需后续迭代

1. **mock/演示数据**：雷达图指标、30 日趋势、净值曲线等使用确定性演示序列（页面已标注），待后端指标历史端点接入后替换
2. **两张表的迁移未建**：`risk_audit_log`（风控审计）、`plan_settlements`（方案结算）——schema 变更需单独走 alembic 迁移
3. **风控跨服务接线**：pause/resume 端点已实现但未与 strategy-service 的策略执行器联动（当前仅更新状态）
4. **screener-service 拆分**：2148 行单体（`domains/screening/service.py`），建议单独立项
5. **预警模块前端页面**：alert-service 8005 无独立页面（设计文档也未覆盖）
6. **`api/core/context.ts`**：零引用死代码，可后续清理

### 真实环境验证建议

- 全栈 SIT：`docker compose up -d` 后对 8004 情绪指数、8010 表预览、8006 风控 dashboard 做一轮真实 PG 联调
- E2E：`npx playwright test` 验证核心链路（登录→看板→选股→方案→风控）

---

## 七、文件变更统计

| 类别 | 新增 | 修改 | 删除 |
|---|---|---|---|
| HTML 预览 | 1（echarts.min.js） | 40+ | 0 |
| 设计文档（.md） | 0 | 60+ | 0 |
| 前端代码 | 18 个子组件/类型/辅助文件 | 20+ | 2（liveTrade.ts, core/http.ts） |
| 后端代码 | 5 个路由/服务文件 + 5 个测试文件 | 5 个 main/routes | 0 |
| 方案文档 | 1（前后端优化建议） + 1（本交接文档） | 1（执行日志追加） | 0 |
