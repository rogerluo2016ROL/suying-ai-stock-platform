# 详细设计 — K线预测·准确率回测

> **关联 PRD**: `5.3 K线预测-准确率回测prd.md` | **Date**: 2026-06-27

## 1. 组件树

```
BacktestView
├── BacktestChart (ECharts: 预测路径虚线 vs 实际走势实线 + 偏差带)
├── AccuracyStats (4宫格: 方向正确率/平均误差/最大误差/最长连对)
├── RecentSequence (最近10次对错: ✅❌可视化)
└── TimeRangeSelector ([近30次] [近60次] [近120次])
```

## 2. ECharts 配置

```typescript
const backtestOption: EChartsOption = {
  series: [
    { type: 'line', name: '30日前预测路径', data: oldPrediction,
      lineStyle: { type: 'dashed', color: 'var(--accent)', width: 2 } },
    { type: 'line', name: '实际走势', data: actualPath,
      lineStyle: { color: 'var(--fg)', width: 2 } },
    { type: 'line', name: '上偏差', data: upperDeviation,
      lineStyle: { opacity: 0 }, areaStyle: { color: 'rgba(61,139,255,0.06)' }, stack: 'dev' },
    { type: 'line', name: '下偏差', data: lowerDeviation,
      lineStyle: { opacity: 0 }, areaStyle: { color: 'transparent' }, stack: 'dev' },
  ]
}
```

## 3. 文件清单

| 文件 | 操作 |
|---|---|
| `frontend/src/components/prediction/BacktestView.tsx` | **新增** |

## 平台化优化补充

> 继承自 `0.3 平台化云端多租户总体需求设计.md`、`0.4 平台化云端多券商架构设计.md`、`0.5 平台化详细方案设计.md`。

| 维度 | 设计要求 |
|---|---|
| 模块定位 | K线预测：公共 Kronos 预测能力与用户私有关注/对比任务分离。 |
| 角色差异 | 操盘手保留专业工作台；个人投资者默认简化解释视图；系统管理员获得配置、审计和运行治理视图。 |
| 数据隔离 | 公共预测快照 + 私有预测任务。所有私有对象必须写入 `tenantId / ownerUserId / accountId? / visibility / dataScope`。 |
| 云端部署 | API 必须从认证上下文恢复租户与用户身份，禁止前端传入越权租户；列表接口默认只返回当前身份可见数据。 |
| 券商/交易 | 预测不可绕过风控直接交易。凡涉及资金、订单、策略启停的动作必须经过 `RiskGate` 并写入 append-only 审计。 |
| 原型要求 | 页面顶部展示角色视图、数据域、账户隔离、券商模式和云端就绪状态；不得把公共行情数据与私有交易数据混在同一区块而不标识。 |

## Changelog
- 2026-06-27: 初稿
