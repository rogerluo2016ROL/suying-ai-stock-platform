# PRD — 模型注册

- **Date**: 2026-06-27 | **Status**: Draft | **角色**: Admin only

## 1. 定位

管理员专用。模型版本管理 + A/B 上线（staging → production 切换）。

## 2. 布局

```
┌─ 📦 模型注册 ──────────────────────────────────────────────────────┐
│                                                                    │
│ ┌─ Production (当前线上) ─────────────────────────────────────────┐│
│ │ Kronos-V2.2 · 上线 06-24 · 准确率 75.1% · 延迟 22ms            ││
│ └──────────────────────────────────────────────────────────────────┘│
│                                                                    │
│ ┌─ Staging (待上线) ──────────────────────────────────────────────┐│
│ │ Kronos-V2.3 · 训练完成 06-27 · 准确率 78.2% · 延迟 18ms        ││
│ │ 提升: +3.1%准确率 · -4ms延迟                                     ││
│ │ [🚀 上线到Production]                                            ││
│ └──────────────────────────────────────────────────────────────────┘│
│                                                                    │
│ ┌─ Archived (已归档) ─────────────────────────────────────────────┐│
│ │ Kronos-V2.1 · 上线 06-10~06-24 · 准确率 73.5%                   ││
│ │ Kronos-V2.0 · 上线 05-20~06-10 · 准确率 71.2%                   ││
│ │ LightGBM-多因子-V3 · 上线 06-15~当前 · 准确率 82.5%             ││
│ └──────────────────────────────────────────────────────────────────┘│
└────────────────────────────────────────────────────────────────────┘
```

## 3. A/B 上线流程

```
Staging模型 → [🚀上线到Production]
  → 当前Production自动归档
  → 新模型切换为Production
  → 通知: "Kronos-V2.3 已上线，准确率78.2%(+3.1%)"
```

## 4. AC

| ID | AC |
|---|---|
| AC-1 | Production/Staging/Archived三级展示正确 |
| AC-2 | A/B上线切换后旧模型自动归档 |
| AC-3 | 非admin返回403 |
| AC-4 | 注册新版本 Modal 可选择 MLflow Run 并自动带回指标/超参数/模型路径，注册后默认为 Staging |
| AC-5 | 版本对比面板展示 A/B 六维指标雷达图（Val Acc/Loss/F1/AUC/Precision/Recall）与逐项差异表，差异方向着色正确 |
| AC-6 | 超参数差异表正确高亮变更项，完全一致时显示空态 |
| AC-7 | Production 版本展示近 30 日生产指标趋势（日度预测准确率 + 滚动 30 日 Sharpe） |

## 平台化优化补充

> 继承自 `0.3 平台化云端多租户总体需求设计.md`、`0.4 平台化云端多券商架构设计.md`、`0.5 平台化详细方案设计.md`。

| 维度 | 设计要求 |
|---|---|
| 模块定位 | 模型注册：模型版本、灰度、回滚和线上标记由管理员治理。 |
| 角色差异 | 操盘手保留专业工作台；个人投资者默认简化解释视图；系统管理员获得配置、审计和运行治理视图。 |
| 数据隔离 | 公共模型注册表 + 管理审计。所有私有对象必须写入 `tenantId / ownerUserId / accountId? / visibility / dataScope`。 |
| 云端部署 | API 必须从认证上下文恢复租户与用户身份，禁止前端传入越权租户；列表接口默认只返回当前身份可见数据。 |
| 券商/交易 | 不触达券商。凡涉及资金、订单、策略启停的动作必须经过 `RiskGate` 并写入 append-only 审计。 |
| 原型要求 | 页面顶部展示角色视图、数据域、账户隔离、券商模式和云端就绪状态；不得把公共行情数据与私有交易数据混在同一区块而不标识。 |

## Changelog
- 2026-06-27: 重写
