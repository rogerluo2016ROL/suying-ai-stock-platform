# 详细设计 — K线预测·多股对比

> **关联 PRD**: `5.2 K线预测-多股对比prd.md` | **Date**: 2026-06-27

## 1. 组件树

```
MultiCompareView
├── CompareTable
│   ├── TableHeader (代码/当前价/预测价/涨幅/置信度/信号一致性/准确率/评级)
│   └── CompareRow × N (点击→曲线高亮, 双击→跳转单股)
├── OverlayChart (ECharts 多线图, 归一化涨跌幅%)
│   └── PredictionLine × N (不同颜色, 选中高亮/其余半透明)
├── SortSelector (综合评级▾/预测涨幅/置信度)
└── BatchActions ([全选] [跳转诊断])
```

## 2. 归一化算法

```typescript
// 不同股价的预测曲线归一化为涨跌幅%, 使可比
function normalizePrediction(stocks: PredictionResult[]) {
  return stocks.map(s => ({
    ...s,
    normalizedPath: s.predictionPath.map((p, i) => 
      ((p - s.currentPrice) / s.currentPrice * 100).toFixed(2)
    )
  }))
}
```

## 3. ECharts 配置

```typescript
const overlayOption: EChartsOption = {
  yAxis: { name: '涨跌幅%', axisLabel: { formatter: '{value}%' } },
  series: stocks.map((s, i) => ({
    type: 'line', name: s.name, data: s.normalizedPath,
    lineStyle: { width: s.selected ? 3 : 1.5 },
    itemStyle: { opacity: s.selected ? 1 : 0.4 }
  }))
}
```

## 4. 文件清单

| 文件 | 操作 |
|---|---|
| `frontend/src/components/prediction/MultiCompareView.tsx` | **新增** |
| `frontend/src/components/prediction/OverlayChart.tsx` | **新增** |

## 平台化优化补充

> 继承自 `0.3 平台化云端多租户总体需求设计.md`、`0.4 平台化云端多券商架构设计.md`、`0.5 平台化详细方案设计.md`。

| 维度 | 设计要求 |
|---|---|
| 模块定位 | K线预测：公共 Kronos 预测能力与用户私有关注/对比任务分离。 |
| 角色差异 | 操盘手保留专业工作台；个人投资者默认简化解释视图；系统管理员获得配置、审计和运行治理视图。 |
| 数据隔离 | 公共预测快照 + 私有预测任务。所有私有对象必须写入 `tenantId / ownerUserId / accountId? / visibility / dataScope`。 |
| 云端部署 | API 必须从认证上下文恢复租户与用户身份，禁止前端传入越权租户；列表接口默认只返回当前身份可见数据。 |
| 券商/交易 | 预测不可绕过风控直接交易。凡涉及资金、订单、策略启停的动作必须经过 `RiskGate` 并写入 append-only 审计。 |
| 原型要求 | 页面顶部展示角色视图、数据域、账户隔离、券商模式和云端就绪状态；不得把公共行情数据与私有交易数据混在同一区块而不标识。 |

## Changelog
- 2026-06-27: 初稿
