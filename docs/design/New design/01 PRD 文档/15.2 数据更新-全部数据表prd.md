# PRD — 数据更新·全部数据表

- **Date**: 2026-06-27 | **Owner**: product-lead | **Status**: Draft
- **父级 PRD**: `15.0 数据更新prd.md`

## 1. 定位

全部数据表是数据更新的**第二个 Tab**（管理员专属）。展示全部 42 张数据表的详细状态，支持逐表/批量手动同步。

| 角色 | 视图 | 操作 |
|---|---|---|
| 管理员 | 全部 42 表 + 筛选 + 手动同步 | 单表同步/批量同步/查看日志 |
| 操盘手 | — | 不可见此 Tab |
| 个人投资者 | — | 不可见此 Tab |

> **角色映射（收口决策）**：管理员 → RBAC `admin`；操盘手 trader → `internal_analyst`；个人投资者 investor → `user`；`external_analyst` → 外部只读。Tab 可见性与写操作权限由服务端按 JWT 角色执行，前端禁止传 `X-User-Role` 等身份头。
>
> **表数量口径（收口决策）**：「42 表（18 核心 + 24 辅助）」为设计示例数，实际表清单以 data-service `GET /api/v1/data/inventory` 返回为准，页面动态渲染。

## 2. 布局

```
┌─ ⏱️ 数据更新 ── 📋全部数据表 ────────────────────────────────────────┐
│                                                                      │
│ ┌─ 筛选栏 ───────────────────────────── [🔄 全部同步] ──────────────┐ │
│ │ 分类: [全部▾]  新鲜度: [全部▾]  搜索: [___________]              │ │
│ └──────────────────────────────────────────────────────────────────┘ │
│                                                                      │
│ ── 核心表 (18) ────────────────────────────────────────────────────  │
│                                                                      │
│ ┌──────────────────────────────────────────────────────────────────┐ │
│ │ 🟢 daily_bars  日K线行情    日K行情 [核心]   2026-06-26  324万行  │ │
│ │ ████████████████████████████████████ 100%                        │ │
│ │ ✅ 上次: 06-26 16:15 · +1,245行 · 12.3s   下次: 18:00          │ │
│ │ [🔄手动同步] [📋日志] [⚙️频率]                                    │ │
│ └──────────────────────────────────────────────────────────────────┘ │
│                                                                      │
│ ┌──────────────────────────────────────────────────────────────────┐ │
│ │ 🟡 fin_basic   财务基础信息  财务数据 [核心]   2026-06-25  8520行  │ │
│ │ ████████████████████░░░░░░░░░░ 66%                               │ │
│ │ ⚠️ 上次: 06-25 18:00 · T-1属季度更新正常                          │ │
│ │ [🔄手动同步] [📋日志]                                              │ │
│ └──────────────────────────────────────────────────────────────────┘ │
│                                                                      │
│ ┌──────────────────────────────────────────────────────────────────┐ │
│ │ 🔴 lhb_detail  龙虎榜明细    公告事件         2026-06-23  4.5万行  │ │
│ │ ████████████░░░░░░░░░░░░░░░░ 33%                                 │ │
│ │ ❌ 上次: 06-23 失败 · Tushare限流·已重试3次                        │ │
│ │ [🔄重试同步] [📋日志] [⏸️暂停]                                     │ │
│ └──────────────────────────────────────────────────────────────────┘ │
│                                                                      │
│ ... (全部 42 表)                                                     │
│                                                                      │
│ ── 辅助表 (24) ────────────────────────────────────────────────────  │
│                                                                      │
│ 🟢 announcements  公告信息    公告事件  2026-06-26  8.5万行  ✅       │
│ 🟡 industry_sw    申万行业    行业分类  2026-06-25  5200行   T-1      │
│ ...                                                                  │
└──────────────────────────────────────────────────────────────────────┘
```

## 3. 新鲜度颜色规则

| 颜色 | 条件 | 含义 |
|---|---|---|
| 🟢 绿色 | `max_date = 今日` | 数据已更新至最新交易日 |
| 🟡 橙色 | `max_date = T-1` | 数据滞后 1 天 |
| 🔴 红色 | `max_date ≤ T-2` 或同步失败 | 需关注 |

## 4. 手动同步流程

1. 点击单表 [🔄手动同步] → 按钮变 Loading
2. POST `/api/v1/data-update/tables/{id}/sync`
3. 成功 → 刷新该表状态 + Toast 通知
4. 失败 → 显示错误原因 + 重试次数

批量同步：[🔄全部同步] → 确认弹窗 → 按 7 阶段顺序同步全部 42 表

## 5. Pill Tab 导航

```
[📊 数据总览] [📋 全部数据表] [⚙️ 同步调度]
                    ↑ active
```

## 6. AC

| ID | AC |
|---|---|
| AC-1 | 管理员可查看全部 42 表，按分类/新鲜度筛选 |
| AC-2 | 新鲜度颜色绿/橙/红正确反映数据滞后天数 |
| AC-3 | 手动同步单表成功刷新状态 |
| AC-4 | 批量全部同步按 7 阶段顺序执行 |
| AC-5 | 操盘手/个人投资者不可见此 Tab |

## 平台化优化补充

> 继承自 `0.3 平台化云端多租户总体需求设计.md`、`0.4 平台化云端多券商架构设计.md`、`0.5 平台化详细方案设计.md`。

| 维度 | 设计要求 |
|---|---|
| 模块定位 | 数据更新：公共数据平台能力，普通用户只看简化新鲜度。 |
| 角色差异 | 操盘手保留专业工作台；个人投资者默认简化解释视图；系统管理员获得配置、审计和运行治理视图。 |
| 数据隔离 | 公共数据质量。所有私有对象必须写入 `tenantId / ownerUserId / accountId? / visibility / dataScope`。 |
| 云端部署 | API 必须从认证上下文恢复租户与用户身份，禁止前端传入越权租户；列表接口默认只返回当前身份可见数据。 |
| 券商/交易 | 不触达券商。凡涉及资金、订单、策略启停的动作必须经过 `RiskGate` 并写入 append-only 审计。 |
| 原型要求 | 页面顶部展示角色视图、数据域、账户隔离、券商模式和云端就绪状态；不得把公共行情数据与私有交易数据混在同一区块而不标识。 |

## Changelog
- 2026-06-27: 首次创建，管理员专属视图
