# PRD — 量化交易·策略配置

- **Date**: 2026-06-27 | **Owner**: product-lead | **Status**: Draft

## 1. 定位

策略配置是**策略参数编辑台**。策略级默认参数 + 标的级覆盖参数。

| 维度 | 全局默认 | 标的覆盖 |
|---|---|---|
| 止损/止盈/仓位 | 对所有标的生效 | 只对覆盖的标生效, null=fallback到全局 |
| 修改方式 | 数字输入+slider | 行内编辑 |

## 2. 布局

```
┌─ ⚙️ 策略配置 ──────────────────────────────────────────────────────┐
│ 编码: USR-FS-V1  名称: [我的自定义策略____________]                  │
│ 选股模型: [匪爷短线多因子 ▾]                                         │
├────────────────────────────────────────────────────────────────────┤
│                                                                    │
│ ┌─ 全局默认参数 (所有标的使用, 除非被覆盖) ────────────────────────┐ │
│ │ 止损: [══○═══════] 3.0%   止盈: [═══════○══] 15.0%             │ │
│ │ 单票仓位: [══○══════] 20%   总仓位: [══════○══] 80%             │ │
│ │ 日最大亏损: [○════════] 3.0%                                     │ │
│ └──────────────────────────────────────────────────────────────────┘ │
│                                                                    │
│ ┌─ 📋 标的清单 (可覆盖全局默认) ───────────────────────────────────┐ │
│ │ [🔄 刷新候选池] [📋 手动添加代码]                                  │ │
│ │                                                                  │ │
│ │ ☑ 代码    名称     止损    止盈    仓位   覆盖状态                 │ │
│ │ ☑ 300750 宁德    [3.0]%  [15.0]% [15]%  ·使用默认               │ │
│ │ ☑ 688981 中芯    [5.0]%  [12.0]% [10]%  ⚙️自定义 (波动大放宽)   │ │
│ │ ☑ 600519 茅台    [2.0]%  [10.0]% [20]%  ⚙️自定义 (稳健收紧)     │ │
│ │ ☐ 000858 五粮    [3.0]%  [15.0]% [20]%  ·使用默认               │ │
│ │                                                                  │ │
│ │ 覆盖 2/4 只 · 总仓位 65% · 预计最大亏损 -2.8%                     │ │
│ │ [全选] [全部恢复默认]                                              │ │
│ │                                                                  │ │
│ │ ┌─ 卖出条件 (全局) ──────────────────────────────────────────┐   │ │
│ │ │ ☑ 止损卖出   ☑ 止盈卖出   ☐ 信号反转   ☐ 时间止盈         │   │ │
│ │ └────────────────────────────────────────────────────────────┘   │ │
│ └──────────────────────────────────────────────────────────────────┘ │
│                                                                    │
│ [💾 保存草稿]  [▶️ 回测]  [🚀 部署上线]                             │
└────────────────────────────────────────────────────────────────────┘
```

## 3. 标的来源

| 来源 | 触发 | 自动带入 |
|---|---|---|
| 选股模型运行 | [🔄 刷新候选池] | 运行选股模型 → 候选标的自动填入 |
| 手动添加 | [📋 手动添加代码] → 输入 300750 | 代码+名称加入清单 |
| 候选池导入 | [📥 从候选池导入] | 读取今日候选池 |

## 4. 覆盖逻辑

```typescript
// 标的存储: null = 使用全局默认
interface StockRiskOverride {
  code: string
  stopLoss: number | null      // null = 使用全局默认
  takeProfit: number | null
  positionPct: number | null
}

// 执行时:
function getEffectiveValue(stock, field) {
  return stock[field] ?? strategy.globalDefaults[field]
}
```

## 5. AC

| ID | AC |
|---|---|
| AC-1 | 全局默认参数区段支持slider+数字输入 |
| AC-2 | 标的清单展示代码/名称/止损/止盈/仓位/覆盖状态 |
| AC-3 | 覆盖参数时行内显示⚙️自定义标记 |
| AC-4 | 未覆盖标的 fallback 全局默认值 |
| AC-5 | [🔄刷新候选池]自动从选股模型拉取标的 |
| AC-6 | [全部恢复默认]清空所有标的覆盖 |

## 平台化优化补充

> 继承自 `0.3 平台化云端多租户总体需求设计.md`、`0.4 平台化云端多券商架构设计.md`、`0.5 平台化详细方案设计.md`。

| 维度 | 设计要求 |
|---|---|
| 模块定位 | 量化交易：策略模板可公共，策略实例、参数和运行日志必须私有隔离。 |
| 角色差异 | 操盘手保留专业工作台；个人投资者默认简化解释视图；系统管理员获得配置、审计和运行治理视图。 |
| 数据隔离 | 公共模板 + 私有策略实例。所有私有对象必须写入 `tenantId / ownerUserId / accountId? / visibility / dataScope`。 |
| 云端部署 | API 必须从认证上下文恢复租户与用户身份，禁止前端传入越权租户；列表接口默认只返回当前身份可见数据。 |
| 券商/交易 | 自动执行绑定账户并经过 RiskGate。凡涉及资金、订单、策略启停的动作必须经过 `RiskGate` 并写入 append-only 审计。 |
| 原型要求 | 页面顶部展示角色视图、数据域、账户隔离、券商模式和云端就绪状态；不得把公共行情数据与私有交易数据混在同一区块而不标识。 |

## Changelog
- 2026-06-27: 重写。增加标的级风险参数覆盖+标的清单
