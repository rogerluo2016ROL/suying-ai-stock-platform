# PRD — 模型训练

- **Date**: 2026-06-27 | **Status**: Draft | **角色**: Admin only

## 1. 定位

管理员专用。管理 Kronos/LightGBM/CatBoost 模型训练任务 + MLflow 实验追踪。

| 维度 | 操盘手 | 个人投资者 | 管理员 |
|---|---|---|---|
| **访问** | ❌ | ❌ | ✅ |
| **核心问题** | — | — | 模型训练完成了吗？效果怎么样？ |

> **角色映射（收口决策）**：管理员（含系统管理员 sys_admin / 模型管理员 model_admin）→ RBAC `admin`；操盘手 trader → `internal_analyst`；个人投资者 investor → `user`；`external_analyst` → 外部只读。权限与数据过滤由服务端按 JWT 角色执行，前端禁止传 `X-User-Role` 等身份头。

## 2. 二级功能

```
🧠 模型训练

├── 📋 训练任务     13.1  任务列表(状态/耗时/指标) + 创建新任务
└── 📈 MLflow       13.2  实验追踪(metrics/params/artifacts)
```

## 3. 布局

### 13.1 训练任务

```
┌─ 📋 训练任务 ──────────────────────────────────────────────────────┐
│ [+ 创建训练任务]                                                     │
├────────────────────────────────────────────────────────────────────┤
│ 任务名称              模型       状态    耗时     loss    acc   时间 │
│ Kronos-V2.3-0627     Kronos     🟢完成   2h15m   0.032  78.2% 09:32│
│ LightGBM-多因子-0626  LightGBM   🟢完成   45m     0.018  82.5% 昨天 │
│ CatBoost-选债-0625    CatBoost   🔴失败   12m     —      —     昨天 │
│ Kronos-V2.2-0624     Kronos     ✅归档   3h10m   0.041  75.1% 06-24│
│                                                                    │
│ 创建任务:                                                           │
│ 模型: [Kronos ▾]  数据集: [全市场2026Q2 ▾]  超参数: [默认 ▾]       │
│ [开始训练]                                                          │
└────────────────────────────────────────────────────────────────────┘
```

### 13.2 MLflow 实验

```
┌─ 📈 MLflow ────────────────────────────────────────────────────────┐
│ 实验: [Kronos-V2.3-0627 ▾]                                        │
├────────────────────────────────────────────────────────────────────┤
│ ┌─ Metrics ──────────────────────────────────────────────────────┐│
│ │ Loss曲线 (ECharts) · Accuracy曲线                               ││
│ └──────────────────────────────────────────────────────────────────┘│
│ ┌─ Parameters ───────────────────────────────────────────────────┐│
│ │ learning_rate: 0.001 · batch_size: 64 · epochs: 100            ││
│ └──────────────────────────────────────────────────────────────────┘│
└────────────────────────────────────────────────────────────────────┘
```

## 4. AC

| ID | AC |
|---|---|
| AC-1 | 训练任务列表展示状态+耗时+指标 |
| AC-2 | 创建任务可选择模型/数据集/超参数 |
| AC-3 | MLflow metrics 曲线正确展示 |
| AC-4 | 非admin返回403 |

## 平台化优化补充

> 继承自 `0.3 平台化云端多租户总体需求设计.md`、`0.4 平台化云端多券商架构设计.md`、`0.5 平台化详细方案设计.md`。

| 维度 | 设计要求 |
|---|---|
| 模块定位 | 模型训练：仅模型管理员/系统管理员可操作，训练资源、实验和产物要隔离。 |
| 角色差异 | 操盘手保留专业工作台；个人投资者默认简化解释视图；系统管理员获得配置、审计和运行治理视图。 |
| 数据隔离 | 管理域数据。所有私有对象必须写入 `tenantId / ownerUserId / accountId? / visibility / dataScope`。 |
| 云端部署 | API 必须从认证上下文恢复租户与用户身份，禁止前端传入越权租户；列表接口默认只返回当前身份可见数据。 |
| 券商/交易 | 不触达券商。凡涉及资金、订单、策略启停的动作必须经过 `RiskGate` 并写入 append-only 审计。 |
| 原型要求 | 页面顶部展示角色视图、数据域、账户隔离、券商模式和云端就绪状态；不得把公共行情数据与私有交易数据混在同一区块而不标识。 |

## Changelog
- 2026-06-27: 重写
