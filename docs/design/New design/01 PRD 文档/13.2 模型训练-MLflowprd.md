# PRD — 模型训练·MLflow

- **Date**: 2026-06-27 | **Status**: Draft | **角色**: Admin only

## 1. 定位

MLflow 是模型训练的**实验追踪视图**。展示 MLflow 实验的 metrics 曲线、超参数、artifacts 和运行历史。

| 维度 | 管理员 (ML工程师) |
|---|---|
| **核心问题** | 当前实验指标怎么样？不同 run 的超参数是什么？有哪些 artifacts？ |
| **操作** | 切换实验、对比 runs、下载 artifacts |

## 2. 核心功能

- 实验选择器下拉切换
- ECharts 指标曲线：Loss (train+val 双线)、Accuracy (train+val 双线)、Learning Rate
- 超参数参数表网格展示
- Artifacts 文件列表 (可下载)
- 近期运行历史列表

> **MLflow 基线界定（收口决策）**：项目默认 `MLFLOW_MODE=mock`，training-service 返回内置模拟实验数据，`/api/v1/training/mlflow/*` 端点在 mock 模式下行为一致；`real` 模式代理真实 MLflow（localhost:5000）。「打开 MLflow Dashboard」外链按钮在 mock 模式下隐藏或置灰。
>
> **角色映射（收口决策）**：管理员（ML 工程师，含系统管理员/模型管理员）→ RBAC `admin`；操盘手 → `internal_analyst`；个人投资者 → `user`；`external_analyst` → 外部只读。权限与数据过滤由服务端按 JWT 角色执行，前端禁止传 `X-User-Role` 等身份头。

## 3. AC

| ID | AC |
|---|---|
| AC-1 | 实验选择器正确切换曲线数据 |
| AC-2 | Loss/Accuracy 双线图表正确渲染 |
| AC-3 | 参数表 grid 显示所有超参数 |
| AC-4 | Artifacts 列表含文件名+大小+时间 |

## 平台化优化补充

> 继承自 `0.3 平台化云端多租户总体需求设计.md`、`0.4 平台化云端多券商架构设计.md`、`0.5 平台化详细方案设计.md`。

| 维度 | 设计要求 |
|---|---|
| 模块定位 | 模型训练：仅模型管理员/系统管理员可操作，训练资源、实验和产物要隔离。 |
| 角色差异 | 操盘手保留专业工作台；个人投资者默认简化解释视图；系统管理员获得配置、审计和运行治理视图。 |
| 数据隔离 | 管理域数据。所有私有对象必须写入 `tenantId / ownerUserId / accountId? / visibility / dataScope`。 |
| 云端部署 | API 必须从认证上下文恢复租户与用户身份，禁止前端传入越权租户；列表接口默认只返回当前身份可见数据。 |
| 券商/交易 | 不触达券商。凡涉及资金、订单、策略启停的动作必须经过 `RiskGate` 并写入 append-only 审计。 |
| 原型要求 | 页面顶部展示角色视图、数据域、账户隔离、券商模式和云端就绪状态；不得把公共行情数据与私有交易数据混在同一区块而不标识。 |

## Changelog
- 2026-06-27: 初稿
