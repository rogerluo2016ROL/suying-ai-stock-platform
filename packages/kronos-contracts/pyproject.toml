[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "kronos-contracts"
version = "0.1.0"
dependencies = ["pydantic>=2"]

[tool.setuptools.packages.find]
where = ["."]
