import type { PlatformSession } from '../../types/platform'

export interface PlatformContextBarProps {
  session: PlatformSession
}

export const PlatformContextBar = (_props: PlatformContextBarProps) => null

export default PlatformContextBar
